---
phase: 02-mascot-module
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/Mascot/markers.ts
  - src/components/Mascot/__tests__/markers.test.ts
  - src/components/Mascot/useIdleScheduler.ts
  - src/components/Mascot/__tests__/useIdleScheduler.test.ts
  - src/components/Mascot/useReducedStimulus.ts
  - src/components/Mascot/__tests__/useReducedStimulus.test.ts
autonomous: true
requirements: [MASC-02]
must_haves:
  truths:
    - "resolveMarkers reads named frame ranges off a require()-d Lottie JSON and tolerates both plain-string and JSON-stringified marker cm encodings (MASC-02, Pitfall 2)"
    - "The idle scheduler fires a weighted-random micro-behavior on a randomized 4-9s interval (12-24s under reduced-stimulus) and pauses while state is not idle (MASC-02, Pitfall 4)"
    - "Reduced-stimulus reflects OS reduce-motion OR the host prop, updating on the OS change event with listener cleanup"
  artifacts:
    - path: "src/components/Mascot/markers.ts"
      provides: "resolveMarkers(asset) -> {name:{startFrame,endFrame}}"
      contains: "resolveMarkers"
    - path: "src/components/Mascot/useIdleScheduler.ts"
      provides: "pausable weighted micro-behavior scheduler + pickWeightedMicroBehavior + nextIdleIntervalMs"
      contains: "useIdleScheduler"
    - path: "src/components/Mascot/useReducedStimulus.ts"
      provides: "OS reduce-motion OR host-prop combiner hook"
      contains: "useReducedStimulus"
  key_links:
    - from: "src/components/Mascot/useIdleScheduler.ts"
      to: "src/components/Mascot/markers.ts"
      via: "resolves idle marker frame ranges to play"
      pattern: "resolveMarkers|MarkerRange"
    - from: "src/components/Mascot/useReducedStimulus.ts"
      to: "AccessibilityInfo"
      via: "isReduceMotionEnabled + reduceMotionChanged listener"
      pattern: "AccessibilityInfo"
---

<objective>
Build the three pure-ish logic units the `<Mascot />` component orchestrates, test-first: marker frame-range resolution (`markers.ts`), the pausable weighted idle micro-behavior scheduler (`useIdleScheduler.ts`), and the reduced-stimulus signal combiner (`useReducedStimulus.ts`). All are unit-testable in isolation against fake timers and mocked native APIs — no LottieView rendering here.

Purpose: Isolate the timing/selection/marker business logic (MASC-02) so it is deterministically testable before it is wired into the component (Plan 04). This directly closes the three MASC-02 Wave 0 test gaps.
Output: markers.ts, useIdleScheduler.ts, useReducedStimulus.ts and their test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mascot-module/02-RESEARCH.md
@.planning/phases/02-mascot-module/02-PATTERNS.md
@.planning/phases/02-mascot-module/02-UI-SPEC.md

<interfaces>
From src/components/Mascot/types.ts (Plan 01):
  MascotState = 'greeting' | 'idle' | 'presence' | 'dozing' | 'acknowledge'
Micro-behaviors (UI-SPEC): 'blink' (50%), 'glance' (30%), 'postureShift' (20%)
Idle interval: 4000-9000ms normal, 12000-24000ms reduced-stimulus
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveMarkers with defensive cm decoding (Pitfall 2)</name>
  <files>src/components/Mascot/markers.ts, src/components/Mascot/__tests__/markers.test.ts</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Pattern 1 resolveMarkers example; Pitfall 2 — JSON-stringified cm names; Assumption A2 — markers survive require())
    - data/repositories/sessions.ts (readRecord try/catch tolerate-malformed idiom with requirement-ID comment — mirror for the defensive JSON.parse fallback)
    - assets/mascot/mascot_idle.json (real asset the test asserts has non-empty markers)
  </read_first>
  <behavior>
    - Test 1: resolveMarkers on a plain-string-cm markers array returns { blink:{startFrame,endFrame}, glance:{...}, postureShift:{...} } with endFrame = tm + dr
    - Test 2: a marker whose cm is JSON-stringified (e.g. cm = '{"name":"blink"}') resolves under key 'blink' (defensive JSON.parse(cm).name, falling back to raw cm on parse failure)
    - Test 3: importing the real assets/mascot/mascot_idle.json and calling resolveMarkers yields all three required names (guards Assumption A2 — markers survive Metro require())
    - Test 4: asset with missing/empty markers returns {} without throwing
  </behavior>
  <action>
    Create `src/components/Mascot/markers.ts` exporting `type MarkerRange = { startFrame: number; endFrame: number }` and `resolveMarkers(asset)` that iterates `asset.markers ?? []`, deriving the key by attempting `JSON.parse(marker.cm).name` inside a try/catch and falling back to the raw `marker.cm` string on failure (mirror sessions.ts's tolerate-malformed try/catch with an inline comment citing Pitfall 2), mapping to `{ startFrame: marker.tm, endFrame: marker.tm + marker.dr }`. Emit a dev-only `console.warn` (never user-facing) if, after resolution, any of the three required names (blink/glance/postureShift) is absent, so a broken final-art drop-in fails loud in logs. Write markers.test.ts covering the four behaviors above (RED first, then implement).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=markers</automated>
  </verify>
  <acceptance_criteria>
    - resolveMarkers returns endFrame === tm + dr for each marker
    - JSON-stringified cm ('{"name":"blink"}') resolves under key 'blink'
    - Real mascot_idle.json resolves all of blink/glance/postureShift
    - Empty/missing markers returns {} and does not throw
    - `npm test -- --testPathPattern=markers` passes
  </acceptance_criteria>
  <done>Marker resolution is defensive against both cm encodings and proven against the real idle asset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weighted, pausable idle micro-behavior scheduler (MASC-02, Pitfall 4)</name>
  <files>src/components/Mascot/useIdleScheduler.ts, src/components/Mascot/__tests__/useIdleScheduler.test.ts</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Code Examples: pickWeightedMicroBehavior, nextIdleIntervalMs; Pitfall 4 — pause on non-idle state)
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (Idle micro-behaviors: weights 50/30/20, intervals 4-9s / 12-24s, never overlap, never during one-shots)
    - i18n/useLocale.ts (hook shape convention: named UseXResult export, no default export)
    - src/components/Mascot/markers.ts (MarkerRange type consumed to know which frames to play)
  </read_first>
  <behavior>
    - Test 1: pickWeightedMicroBehavior with an injected RNG returns 'blink' below 0.5, 'glance' in [0.5,0.8), 'postureShift' at/above 0.8 (boundary-exact, deterministic)
    - Test 2: nextIdleIntervalMs(false) is within [4000,9000); nextIdleIntervalMs(true) is within [12000,24000)
    - Test 3: with fake timers and active=true (state idle), advancing past the max interval invokes the scheduler's play callback exactly once per interval with a valid marker range
    - Test 4 (Pitfall 4): after switching active=false (state greeting/acknowledge), advancing timers does NOT invoke the play callback; returning to active=true resumes scheduling
  </behavior>
  <action>
    Create `src/components/Mascot/useIdleScheduler.ts` exporting `pickWeightedMicroBehavior(random = Math.random)` (cumulative-weight 50/30/20 with floating-point fallback), `nextIdleIntervalMs(reducedStimulus, random = Math.random)` ([4000,9000) normal / [12000,24000) reduced), and a `useIdleScheduler` hook accepting `{ active, reducedStimulus, markers, onPlay }` (or equivalent) that, while `active`, schedules the next micro-behavior via `setTimeout(nextIdleIntervalMs(...))`, on fire weighted-picks a marker, calls `onPlay(markerRange)`, then re-schedules; its effect MUST depend on `active` and clear the pending timeout in cleanup whenever `active` flips false (Pitfall 4 — never fire during a one-shot). Accept an injectable random for deterministic tests. Follow useLocale.ts's named-return/no-default-export convention. Write useIdleScheduler.test.ts with `jest.useFakeTimers()` covering the four behaviors (RED first).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=useIdleScheduler</automated>
  </verify>
  <acceptance_criteria>
    - pickWeightedMicroBehavior respects the 0.5/0.8 boundaries deterministically under an injected RNG
    - nextIdleIntervalMs returns values inside [4000,9000) and [12000,24000)
    - Scheduler fires onPlay while active and is silent while inactive (fake-timer test)
    - Timeout is cleared on active→false transition (no play call during one-shot window)
    - `npm test -- --testPathPattern=useIdleScheduler` passes
  </acceptance_criteria>
  <done>Idle scheduler is weighted, interval-bounded, and provably paused during non-idle states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useReducedStimulus OS-signal + host-prop combiner</name>
  <files>src/components/Mascot/useReducedStimulus.ts, src/components/Mascot/__tests__/useReducedStimulus.test.ts</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Standard Stack — AccessibilityInfo.isReduceMotionEnabled() + addEventListener('reduceMotionChanged', ...).remove() cleanup)
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (Reduced-stimulus contract — logical OR of OS signal and host reducedStimulus prop)
    - i18n/useLocale.ts (hook return-shape convention)
  </read_first>
  <behavior>
    - Test 1: with OS reduce-motion false and host prop undefined/false, hook returns { reducedStimulus: false }
    - Test 2: with host prop true (OS false), returns true (logical OR)
    - Test 3: when the mocked reduceMotionChanged listener fires true, the returned value updates to true
    - Test 4: on unmount, the subscription's remove() is called (no leak)
  </behavior>
  <action>
    Create `src/components/Mascot/useReducedStimulus.ts` exporting `useReducedStimulus(hostProp?: boolean): { reducedStimulus: boolean }`. On mount, read `AccessibilityInfo.isReduceMotionEnabled()` (async) into state and subscribe via `AccessibilityInfo.addEventListener('reduceMotionChanged', ...)`, storing the returned subscription and calling `.remove()` in the effect cleanup. Return `{ reducedStimulus: osReduceMotion || Boolean(hostProp) }`. Test by mocking react-native's AccessibilityInfo (jest.spyOn on isReduceMotionEnabled/addEventListener) covering the four behaviors (RED first).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=useReducedStimulus</automated>
  </verify>
  <acceptance_criteria>
    - Returns the logical OR of OS reduce-motion and the host prop
    - Updates when the mocked reduceMotionChanged event fires
    - Calls subscription.remove() on unmount
    - `npm test -- --testPathPattern=useReducedStimulus` passes
  </acceptance_criteria>
  <done>Reduced-stimulus is a single OR'd signal with proper listener cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled asset → JS marker parser | resolveMarkers consumes an author/artist-controlled Lottie JSON `markers` array |
| OS accessibility API → module | AccessibilityInfo reduce-motion signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | resolveMarkers on malformed/renamed markers | mitigate | Defensive JSON.parse-with-fallback + dev-only console.warn when a required marker is missing (Pitfall 2); empty/missing markers returns {} instead of throwing. |
| T-02-04 | Denial of Service | idle scheduler timer leak / overlap during state change | mitigate | Effect depends on `active`; pending timeout cleared in cleanup on every state change (Pitfall 4), preventing runaway/overlapping play() calls. |
</threat_model>

<verification>
- `npm test -- --testPathPattern="markers|useIdleScheduler|useReducedStimulus"` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- Marker resolution, idle scheduling, and reduced-stimulus logic each pass isolated unit tests
- Scheduler proven to pause during non-idle states (Pitfall 4) and use bounded randomized intervals (MASC-02)
</success_criteria>

<output>
Create `.planning/phases/02-mascot-module/02-03-SUMMARY.md` when done
</output>
