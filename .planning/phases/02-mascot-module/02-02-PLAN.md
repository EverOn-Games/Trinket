---
phase: 02-mascot-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - __mocks__/lottie-react-native.tsx
  - jest.setup.ts
  - scripts/check-mascot-asset-size.mjs
  - assets/mascot/mascot_greeting.json
  - assets/mascot/mascot_idle.json
  - assets/mascot/mascot_presence.json
  - assets/mascot/mascot_dozing.json
  - assets/mascot/mascot_acknowledge.json
autonomous: true
requirements: [MASC-01, MASC-02, MASC-04]
user_setup: []
must_haves:
  truths:
    - "lottie-react-native is installed as a project dependency via `npx expo install` (D-04)"
    - "A Jest mock stands in for LottieView's native render + imperative ref API so component tests run under Node (Claude's Discretion — test strategy)"
    - "Any assets/mascot/*.json over 300KB fails the build via a fail-closed size gate wired into `npm run verify`, and the gate is proven to exit 1 on a synthetic oversized fixture (MASC-04)"
    - "All 5 state slots exist as plain .json Lottie assets with final-art-compatible filenames; mascot_idle.json carries the blink/glance/postureShift markers (MASC-01, MASC-02, D-01, D-02)"
  artifacts:
    - path: "__mocks__/lottie-react-native.tsx"
      provides: "View-stub LottieView with play/pause/resume/reset jest.fn refs"
      contains: "useImperativeHandle"
    - path: "scripts/check-mascot-asset-size.mjs"
      provides: "fail-closed 300KB size gate for assets/mascot/*.json"
      contains: "300"
    - path: "assets/mascot/mascot_idle.json"
      provides: "idle breathing loop carrying blink/glance/postureShift markers"
      contains: "markers"
  key_links:
    - from: "jest.setup.ts"
      to: "__mocks__/lottie-react-native.tsx"
      via: "jest.mock('lottie-react-native')"
      pattern: "lottie-react-native"
    - from: "package.json"
      to: "scripts/check-mascot-asset-size.mjs"
      via: "verify script chains lint:mascot-assets"
      pattern: "check-mascot-asset-size"
---

<objective>
Land the native dependency, test infrastructure, and placeholder assets Phase 2 needs before any module code can render or be tested: install `lottie-react-native` (D-04), create its Jest mock, add the fail-closed 300KB asset size gate (MASC-04), and author the 5 geometric "breathing orb" placeholder Lottie assets with final-art-compatible slot + marker names (D-01/D-02).

Purpose: The module implementation (Plans 03-04) requires a resolvable `lottie-react-native` module, a Jest stand-in for it, and real asset files to `require()`. This plan is the enabling infra.
Output: package.json dependency + verify wiring, `__mocks__/lottie-react-native.tsx`, `scripts/check-mascot-asset-size.mjs`, and `assets/mascot/*.json` (×5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mascot-module/02-RESEARCH.md
@.planning/phases/02-mascot-module/02-PATTERNS.md
@.planning/phases/02-mascot-module/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lottie-react-native + Jest mock (D-04)</name>
  <files>package.json, __mocks__/lottie-react-native.tsx, jest.setup.ts</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Pattern 3 mock code; Pitfall 3 — install by explicit name ONLY, never a bare `expo install` sweep, to avoid downgrading pinned reanimated/worklets)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (__mocks__ section — 4-part doc-comment convention + jest.setup.ts registration shape)
    - __mocks__/react-native-mmkv.ts (doc-comment + jest.fn shape precedent)
    - jest.setup.ts (existing jest.mock registration blocks — append below them)
  </read_first>
  <action>
    Run exactly `npx expo install lottie-react-native` (explicit package name only — NEVER a bare `npx expo install` or `--check` sweep; Pitfall 3: that can "correct"/downgrade the deliberately pinned react-native-reanimated 4.3.1 and react-native-worklets 0.8.3). Confirm the package.json diff adds only lottie-react-native and leaves reanimated/worklets untouched. Create `__mocks__/lottie-react-native.tsx` using RESEARCH Pattern 3: a `forwardRef` component returning a `View` stub with `testID="lottie-view-mock"` that spreads props, exposing `play`/`pause`/`resume`/`reset` as `jest.fn()`s via `useImperativeHandle`; default-export it. Open the file with the repo's 4-part mock doc comment (native capability faked = native LottieView render + imperative ref API; why Jest can't use it = native rendering cannot run under Jest's Node env; what it unblocks = Mascot component/scheduler tests; what still needs hardware = actual on-device animation smoothness). Append `jest.mock('lottie-react-native');` to jest.setup.ts below the existing react-native-mmkv/expo-localization registrations, with a 2-line comment (what's faked, why, what depends on it).
  </action>
  <verify>
    <automated>node -e "process.exit(require('./package.json').dependencies['lottie-react-native']?0:1)" && npm test -- --testPathPattern=tokens</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` includes `lottie-react-native`
    - package.json still pins `react-native-reanimated: 4.3.1` and `react-native-worklets: 0.8.3` unchanged
    - `__mocks__/lottie-react-native.tsx` default-exports a forwardRef component exposing play/pause/resume/reset as jest.fn(), with testID `lottie-view-mock` and the 4-part doc comment
    - jest.setup.ts contains `jest.mock('lottie-react-native');`
  </acceptance_criteria>
  <done>lottie-react-native installed by explicit name; Jest mock created and registered; reanimated/worklets pins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed mascot asset size gate (MASC-04)</name>
  <files>scripts/check-mascot-asset-size.mjs, package.json</files>
  <read_first>
    - scripts/check-hex-literals.mjs (verbatim structural template: shebang, root via import.meta.url, empty-scan-fails, violation accumulator, process.exit(0|1))
    - package.json (scripts block — how lint:hex is wired into verify)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (check-mascot-asset-size.mjs section — the 5 elements to copy verbatim)
    - data/repositories/__tests__/schema.denylist.test.ts (Phase 1 precedent for PROVING a fail-closed guard actually catches a violation via a temporary probe — mirror that "inject a known-bad artifact, assert the gate rejects it, clean up" discipline)
  </read_first>
  <action>
    Create `scripts/check-mascot-asset-size.mjs` mirroring check-hex-literals.mjs's fail-closed shape: `#!/usr/bin/env node` shebang; resolve ROOT via `import.meta.url` (not process.cwd); SCAN_GLOB `assets/mascot/*.json`; MAX_BYTES `300 * 1024`; if `files.length === 0` print an error and `process.exit(1)` (empty scan is a FAILURE, never a silent pass); for each file `statSync` and accumulate `violationCount` for any size greater than MAX_BYTES with a per-file error line naming the offending file path and citing MASC-04; on violations print a summary and `process.exit(1)`; else success `console.log` + `process.exit(0)`. Doc-comment the file citing MASC-04 and the 300KB contract. Wire into package.json: add `"lint:mascot-assets": "node scripts/check-mascot-asset-size.mjs"` and update `"verify"` to `"npm run lint && npm run lint:hex && npm run lint:mascot-assets && npm test"`.

    THEN prove the gate is genuinely fail-closed (the point of a fail-closed gate is worthless unless the failing branch is exercised): the verify command below drives the script through BOTH branches using a temporary probe asset it creates and deletes — (a) a valid `{}` probe under 300KB must exit 0 (success path), and (b) a synthetic >300KB probe must exit 1 AND print a per-file violation line naming the probe. This mirrors how Phase 1's schema.denylist test injects a known-bad field to prove the denylist actually rejects it. Note the empty-scan-fails branch is asserted by source inspection (acceptance criterion) because at this task's execution time the 5 real assets do not exist yet — they are authored in Task 3, whose own verify exercises the real-asset success path. Do NOT terminate the verify with an unconditional `echo "exit=$?"` (or any trailing command that always exits 0) — that would swallow a real non-zero exit and mask a broken gate; every stage must propagate its exit code.
  </action>
  <verify>
    <automated>mkdir -p assets/mascot && printf '{}' > assets/mascot/__probe.json && node scripts/check-mascot-asset-size.mjs && node -e "require('fs').writeFileSync('assets/mascot/__probe.json','x'.repeat(300*1024+1))" && { OUT=$(node scripts/check-mascot-asset-size.mjs 2>&1); RC=$?; rm -f assets/mascot/__probe.json; echo "$OUT"; [ $RC -eq 1 ] && echo "$OUT" | grep -q '__probe.json' && echo GATE_FAILS_CLOSED; }</automated>
  </verify>
  <acceptance_criteria>
    - Running the script against a valid <300KB probe prints the success line and exits 0 (success path proven, independent of the yet-unwritten real assets)
    - Running the script against a synthetic >300KB probe exits 1 AND prints a per-file MASC-04 violation line naming the probe file (fail-closed on oversize is PROVEN, not merely coded) — the verify command emits `GATE_FAILS_CLOSED` only when both hold
    - The verify command propagates real exit codes at every stage — no trailing unconditional `echo`/command that forces exit 0
    - The `files.length === 0` branch calls process.exit(1) (empty-scan-fails) — visible in source
    - package.json `scripts.verify` chains `lint:mascot-assets` before `npm test`
    - `scripts/check-mascot-asset-size.mjs` resolves ROOT via `import.meta.url`
    - The temporary `assets/mascot/__probe.json` fixture is removed after the check (no probe artifact left committed)
  </acceptance_criteria>
  <done>A fail-closed 300KB gate over assets/mascot/*.json is wired into `npm run verify` AND proven to exit 1 with a violation line on a synthetic oversized fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Author 5 placeholder Lottie assets (D-01, D-02, MASC-01, MASC-02)</name>
  <files>assets/mascot/mascot_greeting.json, assets/mascot/mascot_idle.json, assets/mascot/mascot_presence.json, assets/mascot/mascot_dozing.json, assets/mascot/mascot_acknowledge.json</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Pattern 2 minimal Bodymovin JSON example; the fill-color note — [0.949,0.788,0.533,1] is #F2C988; the "hex in JSON asset is a deliberate exception to the hex gate" note; Pitfall 1 — never .lottie)
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (State→slot mapping table + Idle micro-behaviors table: marker names blink/glance/postureShift, clip lengths, per-state motion signatures)
    - .planning/phases/02-mascot-module/02-CONTEXT.md (D-01 abstract geometric breathing orb; D-02 exact filenames + marker names)
  </read_first>
  <action>
    Author 5 valid hand-written minimal Bodymovin JSON files at exact paths `assets/mascot/mascot_<state>.json` for state in {greeting, idle, presence, dozing, acknowledge}, each a soft geometric "breathing orb": one ellipse shape layer, fill color `[0.949, 0.788, 0.533, 1]` (mascotGlow #F2C988 as 0–1 RGBA — this color literal lives inside the JSON asset only, a deliberate exception the hex gate does not scan), top-level fields v/fr/ip/op/w/h/nm/layers per Pattern 2, canvas ~220×220. Give each a distinct motion signature via scale/opacity keyframes: greeting = brief rise/bloom, idle = slow continuous breathe, presence = steady warm pulse, dozing = slower + dimmer breathe (lower opacity), acknowledge = single gentle bounce/settle. ONLY `mascot_idle.json` carries a top-level `markers` array with exactly three entries whose `cm` values are `blink`, `glance`, `postureShift`, each with `tm` (start frame) and `dr` (duration frames) sized to the UI-SPEC clip lengths (blink ~0.3–0.5s, glance ~0.8–1.2s, postureShift ~1–2s at the file's `fr`). Use plain `.json` only — never the `.lottie` binary format (Pitfall 1: breaks on Android New Architecture). Keep every file well under 300KB.
  </action>
  <verify>
    <automated>node scripts/check-mascot-asset-size.mjs && node -e "const m=require('./assets/mascot/mascot_idle.json').markers.map(x=>x.cm).sort(); process.exit(JSON.stringify(m)===JSON.stringify(['blink','glance','postureShift'])?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at `assets/mascot/mascot_<state>.json` and parse as valid JSON
    - Each file has top-level `v`, `fr`, `ip`, `op`, `w`, `h`, `layers`
    - `mascot_idle.json` `markers` array has exactly cm values blink, glance, postureShift (each with numeric tm + dr)
    - No `.lottie` file is introduced anywhere
    - `node scripts/check-mascot-asset-size.mjs` exits 0 (real-asset success path — the 5 assets now present, all under 300KB)
  </acceptance_criteria>
  <done>5 plain-.json geometric placeholder assets exist with correct slot names; idle carries the 3 named markers; all under the 300KB gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | Installing a net-new native dependency (lottie-react-native) |
| bundled asset → native Lottie renderer | Lottie JSON parsed/decoded on-device (low-end Android) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm install of lottie-react-native | mitigate | Package pre-slopchecked OK in RESEARCH.md Package Legitimacy Audit (registry ground truth + tarball inspection + slopcheck 0.6.1 OK). Install by explicit name only via `npx expo install` (Pitfall 3) so no other pinned versions are silently mutated. |
| T-02-01 | Denial of Service | oversized/malformed animation JSON on low-end Android | mitigate | Fail-closed 300KB size gate (this plan) bounds asset size and is PROVEN to exit 1 on a synthetic >300KB probe (Task 2 verify); lazy per-state loading (Plan 04) bounds worst-case memory; plain .json avoids the .lottie New-Arch parse crash (Pitfall 1). |
</threat_model>

<verification>
- Task 2 gate proof emits `GATE_FAILS_CLOSED` (valid probe → exit 0, oversized probe → exit 1 + violation line)
- After Task 3, `node scripts/check-mascot-asset-size.mjs` exits 0 with the 5 real assets present
- `npm test` green (mock resolves; existing suite unaffected)
- package.json reanimated/worklets pins unchanged
</verification>

<success_criteria>
- lottie-react-native installed; Jest mock registered
- 300KB fail-closed gate wired into `npm run verify` and proven to reject an oversized fixture
- 5 placeholder assets exist with correct slot + marker names, all plain .json, all under 300KB
</success_criteria>

<output>
Create `.planning/phases/02-mascot-module/02-02-SUMMARY.md` when done
</output>
