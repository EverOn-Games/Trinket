---
phase: 02-mascot-module
plan: 05
type: execute
wave: 4
depends_on: [02-02, 02-04]
files_modified:
  - i18n/locales/en.json
  - i18n/locales/pl.json
  - src/app/index.tsx
  - src/app/__tests__/screens.test.tsx
autonomous: false
requirements: [MASC-01, MASC-04]
user_setup: []
must_haves:
  truths:
    - "The Home screen mounts the real <Mascot /> (replacing MascotSlot), greeting once per cold launch then resting in idle (MASC-01, D-07)"
    - "Greeting cadence uses an in-memory app-session flag only — no persisted last-greeted timestamp anywhere (D-07)"
    - "Per-state accessibility labels exist under mascot.accessibility.* in both en and pl, warm/plain/gender-neutral, reusing the 'towarzysz'/'companion' vocabulary"
    - "The mascot animates without visible stutter on real low/mid-tier Android hardware, verified on device (MASC-04, D-03/D-04); iOS device verification stays deferred as a carried blocker"
  artifacts:
    - path: "src/app/index.tsx"
      provides: "Home hub mounting <Mascot /> with greeting-once-per-launch"
      contains: "Mascot"
    - path: "i18n/locales/pl.json"
      provides: "mascot.accessibility.* Polish labels"
      contains: "accessibility"
  key_links:
    - from: "src/app/index.tsx"
      to: "src/components/Mascot/Mascot.tsx"
      via: "renders <Mascot state=... accessibilityLabel={t('mascot.accessibility.*')} />"
      pattern: "Mascot"
    - from: "src/app/index.tsx"
      to: "i18n/locales/en.json"
      via: "t('mascot.accessibility.idle') etc."
      pattern: "mascot.accessibility"
---

<objective>
Complete the vertical slice: swap the Phase 1 `MascotSlot` placeholder on the Home hub for the real `<Mascot />` (greeting once per cold launch, then idle), add the per-state accessibility copy in both locales, and verify animation smoothness on real Android hardware (D-03/D-04). After this plan, a user opening Trinket actually sees the calm companion breathing on Home.

Purpose: Wire the module into its first host and prove MASC-04's on-device smoothness — the phase's user-observable payoff.
Output: i18n keys, updated Home screen + screen test, and a passed Android device checkpoint. Native inputs change (lottie-react-native), so the phase completion message MUST carry the prebuild note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mascot-module/02-PATTERNS.md
@.planning/phases/02-mascot-module/02-UI-SPEC.md
@.planning/phases/02-mascot-module/02-CONTEXT.md

<interfaces>
From src/components/Mascot/Mascot.tsx (Plan 04): <Mascot state? prominence? size? reducedStimulus? accessibilityLabel(required) onStateAnimationComplete? testID? />
Existing Home (src/app/index.tsx): mounts <MascotSlot accessibilityLabel={t('home.mascotSlotLabel')} /> inside a ScrollView; StyleSheet.flatten never-array rule applies at route root
Existing pl vocabulary: home.mascotSlotLabel = "Miejsce twojego towarzysza" (towarzysz = companion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mascot.accessibility.* copy (en + pl)</name>
  <files>i18n/locales/en.json, i18n/locales/pl.json</files>
  <read_first>
    - i18n/locales/en.json (existing top-level namespaces; home.mascotSlotLabel)
    - i18n/locales/pl.json (existing warm/plain/gender-neutral register; home.mascotSlotLabel = "Miejsce twojego towarzysza")
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (Copywriting Contract — recommended EN default per-state labels, present-tense/reactive, never directive)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (i18n section — new top-level "mascot" namespace, no pluralization needed, reuse "towarzysz")
  </read_first>
  <action>
    Add a new top-level `mascot` namespace (sibling to `home`) with a nested `accessibility` object holding keys greeting/idle/presence/dozing/acknowledge in BOTH en.json and pl.json. EN defaults per UI-SPEC: greeting "Your companion says hello", idle "Your companion is here, resting quietly", presence "Your companion is here with you", dozing "Your companion has dozed off", acknowledge "Your companion acknowledges you". PL: warm/plain/gender-neutral register reusing "towarzysz" (companion) consistent with home.mascotSlotLabel — present-tense, reactive, never directive/demanding (PDA grammar). These are static labels — do NOT apply the _one/_few/_many CLDR plural pattern (not counted quantities). Keep the two files structurally parallel (same key set).
  </action>
  <verify>
    <automated>node -e "const en=require('./i18n/locales/en.json').mascot.accessibility, pl=require('./i18n/locales/pl.json').mascot.accessibility; const k=['greeting','idle','presence','dozing','acknowledge']; process.exit(k.every(x=>en[x]&&pl[x])?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - en.json and pl.json each have mascot.accessibility.{greeting,idle,presence,dozing,acknowledge} as non-empty strings
    - PL strings reuse "towarzysz" vocabulary and contain no directive/imperative phrasing
    - No _one/_few/_many suffixed keys added for this namespace
  </acceptance_criteria>
  <done>Per-state accessibility copy exists in both locales, warm/non-directive, structurally parallel.</done>
</task>

<task type="auto">
  <name>Task 2: Swap MascotSlot → Mascot on Home with greeting-once-per-launch (D-07)</name>
  <files>src/app/index.tsx, src/app/__tests__/screens.test.tsx</files>
  <read_first>
    - src/app/index.tsx (current MascotSlot mount, imports, StyleSheet.flatten never-array rule, ScrollView layout)
    - src/components/Mascot/Mascot.tsx (public props; default idle)
    - src/app/__tests__/screens.test.tsx (existing Home assertions + async render discipline)
    - .planning/phases/02-mascot-module/02-CONTEXT.md (D-07 in-memory greeting flag; module never reads settings — host passes prominence)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (Schema denylist guard section — greeting flag must be in-memory only, never through settingsRepo/useSettingsStore)
  </read_first>
  <action>
    In src/app/index.tsx, replace the `MascotSlot` import + usage with `<Mascot />`. Manage greeting cadence with an IN-MEMORY app-session flag ONLY (D-07): e.g. a module-level `let hasGreetedThisSession = false` (or a non-persisted store) — on first Home mount of a cold launch, set state='greeting' and pass `onStateAnimationComplete` to advance to state='idle' and mark the flag; on subsequent mounts render state='idle' directly. NEVER persist a last-greeted timestamp and never route this through settingsRepo/useSettingsStore (would trip the schema denylist intent). Pass `accessibilityLabel={t('mascot.accessibility.<currentState>')}` matching the current state, and `prominence` read from `settingsRepo.get().mascotProminence` (host reads the setting; module stays feature-agnostic). Keep the ScrollView layout and the StyleSheet.flatten never-array rule intact. Update screens.test.tsx: assert Home renders the mascot (query the lottie-view-mock via the mounted <Mascot />) and remove/replace any assertion tied to the old MascotSlot testID. Do not delete MascotSlot.tsx (its treatment is reused inside Mascot as the fallback box); just stop importing it in Home.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=screens && npm run lint:hex</automated>
  </verify>
  <acceptance_criteria>
    - src/app/index.tsx imports and renders `<Mascot />`, not `<MascotSlot />`
    - Greeting cadence uses an in-memory flag; grep of src/app/index.tsx shows no `settingsRepo`/`useSettingsStore` write of any greeted/timestamp field and no `lastGreetedAt`/`greetedAt` identifier
    - Home passes prominence from settingsRepo.get().mascotProminence and a state-matched accessibilityLabel via t()
    - screens.test.tsx passes and asserts the mascot mounts on Home
    - `npm run lint:hex` passes
  </acceptance_criteria>
  <done>Home mounts the real mascot, greeting once per cold launch via an in-memory flag, resting in idle thereafter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Android device smoothness verification (MASC-04, D-03/D-04)</name>
  <files>(no source files — human on-device verification of the already-built module)</files>
  <action>Human-only verification of MASC-04 smoothness on real Android hardware, per the how-to-verify steps below. Because lottie-react-native is a net-new native dependency, the native project must be regenerated (`npx expo prebuild --clean`) and the Android dev client rebuilt (`npm run android:fresh`) before observing. This step cannot be automated — perceptual frame-drop/stutter cannot be asserted in Jest (see 02-VALIDATION.md Manual-Only Verifications). iOS stays out of scope (D-03).</action>
  <what-built>
    The full mascot module is wired into Home: greeting-then-idle with 3 randomized micro-behaviors (blink/glance/postureShift), single persistent LottieView, Reanimated fades, all placeholder geometric "breathing orb" assets under 300KB. lottie-react-native is a net-new native dependency, so the native project must be regenerated and rebuilt.
  </what-built>
  <how-to-verify>
    1. Pull the branch. Because native inputs changed (lottie-react-native added), run `npx expo prebuild --clean` (or `npm run android:fresh`) then build/run the dev client on your real low/mid-tier Android hardware (Windows, `npm run android:fresh`).
    2. On the Home screen, watch the mascot for at least 60 seconds in idle.
    3. Confirm: the greeting plays once on cold launch then settles into a calm idle breathe; 3 distinct micro-behaviors (blink, glance, posture shift) appear on relaxed, non-fixed intervals (roughly every 4-9s); the idle loop and micro-behaviors show NO visible stutter or frame drop; the orb glow is the amber mascotGlow tone; nothing feels attention-grabbing or demanding (PDA/shame-free).
    4. Optionally toggle OS reduce-motion and confirm micro-behaviors space out (12-24s) and transitions feel calmer/slower, not removed.
    5. iOS is intentionally OUT OF SCOPE this phase (D-03) — do NOT attempt iOS verification; the iOS device-boot gate remains a carried blocker for Phase 9.
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual on-device smoothness (MASC-04) is human-verify only per 02-VALIDATION.md; no Jest assertion possible. Automated coverage for the module is in Plans 03-04 + `npm run verify`.</automated>
    <human-check>On real low/mid-tier Android: idle loop + 3 micro-behaviors + greeting/idle transition show no visible stutter or frame drop over 60s.</human-check>
  </verify>
  <done>User confirms the mascot animates smoothly and calmly on Android hardware (MASC-04); iOS verification remains a deferred, carried blocker (D-03).</done>
  <resume-signal>Type "approved" if smooth and calm on Android, or describe any stutter/jank/visual issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host (Home) → module | Home supplies state/prominence/accessibilityLabel; reads mascotProminence from local settings |
| in-memory session flag | Greeting cadence state lives only in process memory, never persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | greeting cadence as a disguised aggregate | mitigate | Greeting flag is in-memory only (D-07); no lastGreetedAt/timestamp persisted — keeps the schema denylist intent intact. Enforced by acceptance-criteria grep. |
| T-02-01 | Denial of Service | animation jank / oversized asset on low-end Android | mitigate | Device checkpoint verifies real-hardware smoothness (MASC-04); size gate + lazy load already bound asset/memory cost. |
</threat_model>

<verification>
- `npm test -- --testPathPattern=screens` green
- `npm run verify` green (lint + hex gate + mascot-asset gate + jest)
- Android device checkpoint approved (MASC-04 smoothness)
</verification>

<success_criteria>
- Home shows the real mascot: greeting once per cold launch, then calm idle with 3 micro-behaviors
- No persisted greeting timestamp (D-07)
- Accessibility copy present in en + pl, non-directive
- Android on-device smoothness verified; iOS verification deferred (blocker carried, not closed)
</success_criteria>

<output>
Create `.planning/phases/02-mascot-module/02-05-SUMMARY.md` when done.
The SUMMARY MUST include the CLAUDE.md agent-rule note: "Native inputs changed (lottie-react-native added) — run `npx expo prebuild --clean` after pulling" for the orchestrator to surface in the phase completion message.
</output>
