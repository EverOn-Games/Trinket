---
phase: 02-mascot-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - theme/tokens.ts
  - theme/__tests__/tokens.test.ts
  - src/components/Mascot/types.ts
  - src/components/Mascot/__tests__/noNegativeStates.test.ts
  - data/types.ts
  - data/stores/useSettingsStore.ts
  - data/repositories/settings.ts
  - data/repositories/__tests__/repositories.test.ts
  - data/repositories/__tests__/schema.denylist.test.ts
autonomous: true
requirements: [MASC-01, MASC-03]
must_haves:
  truths:
    - "The 5-value MascotState union and MascotProps public API exist as the contract every later plan imports (MASC-01)"
    - "No 6th mascot state and no negative/sad/disappointed state literal can exist without failing a source-scan test (MASC-03, D-01)"
    - "colors.mascotGlow ('#F2C988') exists as the mascot-only accent token and passes the hex-format token test (D-08)"
    - "settings carries mascotProminence (default 'prominent') persisted in the existing settings MMKV blob, and the schema denylist guard still passes with it present (D-05)"
  artifacts:
    - path: "src/components/Mascot/types.ts"
      provides: "MascotState, MascotProminence, MascotSize, MascotProps public contract"
      contains: "MascotState"
    - path: "theme/tokens.ts"
      provides: "mascotGlow token in ThemeTokens.colors + darkTokens.colors"
      contains: "mascotGlow"
    - path: "data/types.ts"
      provides: "mascotProminence field on SettingsState"
      contains: "mascotProminence"
  key_links:
    - from: "data/types.ts"
      to: "src/components/Mascot/types.ts"
      via: "import type { MascotProminence }"
      pattern: "MascotProminence"
    - from: "theme/__tests__/tokens.test.ts"
      to: "theme/tokens.ts"
      via: "REQUIRED_COLOR_KEYS includes 'mascotGlow'"
      pattern: "mascotGlow"
---

<objective>
Establish the Phase 2 contracts every later plan builds against: the public `<Mascot />` type API, the mascot-only `mascotGlow` accent token, and the additive `mascotProminence` settings field. This is interface-first work — no rendering, no animation, no host wiring yet. Downstream plans import these contracts rather than exploring the codebase.

Purpose: Lock the state union (MASC-03's structural "no 6th state" guarantee) and the settings field (D-05) before any implementation depends on them.
Output: `src/components/Mascot/types.ts`, extended theme tokens, extended settings store/repo, and their guard tests.
</objective>

<phase_goal>
**As an** adult with ADHD, **I want to** open Trinket and find a calm companion already present, **so that** starting feels like arriving somewhere with a presence rather than opening an empty tool.

(Derived faithfully from the documented core value in CLAUDE.md — the ROADMAP `**Goal:**` line for Phase 2 is outcome-shaped, not literal user-story format. If a canonical story is wanted, run `/gsd mvp-phase 2`.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mascot-module/02-UI-SPEC.md
@.planning/phases/02-mascot-module/02-PATTERNS.md
@.planning/phases/02-mascot-module/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mascotGlow accent token (D-08)</name>
  <files>theme/tokens.ts, theme/__tests__/tokens.test.ts</files>
  <read_first>
    - theme/tokens.ts (add one key to ThemeTokens.colors type block and to darkTokens.colors)
    - theme/__tests__/tokens.test.ts (extend REQUIRED_COLOR_KEYS array; existing it.each asserts hex format)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (theme/tokens.ts section — exact placement, and the "do NOT add mascotGlowDeep" rule)
  </read_first>
  <action>
    Add `mascotGlow: string;` to the `ThemeTokens.colors` type (as the 9th color key, after `border`) with an inline comment `// '#F2C988' — amber/gold, mascot-only accent (D-08)`. Add `mascotGlow: '#F2C988',` to `darkTokens.colors` (after `border`). Do NOT add `mascotGlowDeep` — D-08 explicitly defers it. Extend the `REQUIRED_COLOR_KEYS` array in theme/__tests__/tokens.test.ts to include `'mascotGlow'` so the existing hex-format `it.each` covers it automatically. This hex value belongs in theme/tokens.ts only; the same value later appears inside Lottie JSON assets (Plan 02), which is a deliberate, documented exception to the hex-literal gate.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=tokens</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeTokens.colors` declares `mascotGlow: string`
    - `darkTokens.colors.mascotGlow === '#F2C988'`
    - `REQUIRED_COLOR_KEYS` in tokens.test.ts contains `'mascotGlow'`
    - `mascotGlowDeep` does NOT appear anywhere in theme/tokens.ts
    - `npm test -- --testPathPattern=tokens` passes
  </acceptance_criteria>
  <done>mascotGlow token added and covered by the existing hex-format test; mascotGlowDeep not present.</done>
</task>

<task type="auto">
  <name>Task 2: Define Mascot public type contract + MASC-03 structural guard</name>
  <files>src/components/Mascot/types.ts, src/components/Mascot/__tests__/noNegativeStates.test.ts</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (Public component API block — exact MascotState/MascotProminence/MascotSize/MascotProps shapes; note "deliberately no style prop")
    - data/types.ts (typed-union/interface convention to mirror)
    - data/repositories/__tests__/schema.denylist.test.ts (source-scan test shape: readFileSync + regex extraction + expect([]) — mirror for the state-union guard)
  </read_first>
  <action>
    Create `src/components/Mascot/types.ts` exporting exactly: `MascotState = 'greeting' | 'idle' | 'presence' | 'dozing' | 'acknowledge'` (exactly these 5, in this order); `MascotProminence = 'prominent' | 'subtle' | 'hidden'`; `MascotSize = 'lg' | 'md' | 'sm'`; and `MascotProps` per the UI-SPEC (state? default idle, prominence? default prominent, size?, reducedStimulus?, accessibilityLabel required, onStateAnimationComplete?, testID?). Deliberately NO `style` prop (UI-SPEC locks size to presets). Create `src/components/Mascot/__tests__/noNegativeStates.test.ts` (MASC-03): read types.ts raw source with readFileSync, extract the MascotState union string literals via regex, assert the set equals exactly the 5 allowed values and contains no additional/negative literal (e.g. 'sad', 'disappointed', 'waiting', 'nagging'). Follow schema.denylist.test.ts's source-scan idiom (readFileSync(join(__dirname, ...)) + regex + expect(...).toEqual(expected)).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=noNegativeStates</automated>
  </verify>
  <acceptance_criteria>
    - types.ts exports `MascotState` with exactly 5 members: greeting, idle, presence, dozing, acknowledge
    - `MascotProps` has required `accessibilityLabel: string` and no `style` property
    - noNegativeStates.test.ts asserts the union set equals the 5 allowed values and fails if a 6th literal is added
    - `npm test -- --testPathPattern=noNegativeStates` passes
    - `npx tsc --noEmit` reports no errors introduced by types.ts
  </acceptance_criteria>
  <done>Public Mascot type contract exists; a source-scan test structurally forbids any 6th/negative state (MASC-03).</done>
</task>

<task type="auto">
  <name>Task 3: Add mascotProminence to settings store/repo (D-05)</name>
  <files>data/types.ts, data/stores/useSettingsStore.ts, data/repositories/settings.ts, data/repositories/__tests__/repositories.test.ts, data/repositories/__tests__/schema.denylist.test.ts</files>
  <read_first>
    - data/types.ts (SettingsState interface — add one field)
    - data/stores/useSettingsStore.ts (existing notificationsOptIn field + setNotificationsOptIn action — mirror exactly)
    - data/repositories/settings.ts (get destructure + update if-branch pattern — mirror)
    - data/repositories/__tests__/repositories.test.ts and schema.denylist.test.ts (extend coverage; do not create new files)
    - .planning/phases/02-mascot-module/02-PATTERNS.md (data/types.ts / useSettingsStore / settings.ts section — exact wiring and the import-MascotProminence instruction)
  </read_first>
  <action>
    Extend `SettingsState` in data/types.ts with `mascotProminence: MascotProminence;`, importing the type via `import type { MascotProminence } from '../src/components/Mascot/types';` (confirm the correct relative path from data/types.ts at edit time — reuse the existing union, do not redeclare it). In useSettingsStore.ts: add `mascotProminence: MascotProminence` to `SettingsStoreState`, initialize `mascotProminence: 'prominent'`, and add `setMascotProminence: (mascotProminence) => set({ mascotProminence })` following the exact `setNotificationsOptIn` shape (same persist blob, no new storage key). In settings.ts repo: add `mascotProminence` to the `get()` destructure/return and add an `if (patch.mascotProminence !== undefined) { useSettingsStore.getState().setMascotProminence(patch.mascotProminence); }` branch in `update()` mirroring the notificationsOptIn branch. Extend repositories.test.ts to assert default `'prominent'` and round-trip of `settingsRepo.update({ mascotProminence: 'subtle' })`. Confirm schema.denylist.test.ts still passes (mascotProminence contains no denylisted stem). Do NOT add any greeting/last-greeted timestamp field anywhere (D-07).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="repositories|schema.denylist"</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsState.mascotProminence` typed as the imported `MascotProminence` (not a redeclared inline union)
    - `useSettingsStore` initializes `mascotProminence: 'prominent'` and exposes `setMascotProminence`
    - `settingsRepo.get()` returns `mascotProminence`; `settingsRepo.update({ mascotProminence: 'hidden' })` persists it
    - schema.denylist.test.ts passes with the new field present
    - No `lastGreetedAt`/`greetedAt`/streak/daily field added anywhere (grep returns nothing)
    - `npm test -- --testPathPattern="repositories|schema.denylist"` passes
  </acceptance_criteria>
  <done>mascotProminence persists in the existing settings blob with default 'prominent', reusing the Mascot type union; denylist guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → module props | Host screens pass `MascotProps` (state/prominence) into the module; TS narrows at compile time |
| app code → MMKV settings blob | `mascotProminence` is written to on-device local storage only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | MascotState union | mitigate | Union pinned to exactly 5 values; noNegativeStates source-scan test fails build if a 6th/negative literal is added (MASC-03). Runtime clamp of unknown state → idle lands in Plan 04 (V5). |
| T-02-02 | Information Disclosure | mascotProminence field | accept | Enum-only preference value, no PII, local-only; covered by existing schema denylist guard. |
</threat_model>

<verification>
- `npm test -- --testPathPattern="tokens|noNegativeStates|repositories|schema.denylist"` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- Mascot type contract, mascotGlow token, and mascotProminence settings field all exist and are test-guarded
- No 6th/negative mascot state can be introduced without a failing test (MASC-03)
- No persisted greeting timestamp introduced (D-07)
</success_criteria>

<output>
Create `.planning/phases/02-mascot-module/02-01-SUMMARY.md` when done
</output>
