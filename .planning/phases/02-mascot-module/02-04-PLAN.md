---
phase: 02-mascot-module
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/components/Mascot/__tests__/Mascot.test.tsx
  - src/components/Mascot/Mascot.tsx
autonomous: true
requirements: [MASC-01, MASC-03, MASC-04]
must_haves:
  truths:
    - "<Mascot /> renders each of the 5 states via the correct mascot_<state>.json source on a single persistent LottieView (MASC-01, MASC-04)"
    - "Exactly one LottieView is mounted per instance at all times, including across rapid state changes — no two-instance crossfade (MASC-04)"
    - "One-shot states (greeting, acknowledge) fire onStateAnimationComplete(state); idle drives micro-behaviors via the scheduler calling the LottieView's play() (MASC-01, MASC-02)"
    - "State assets load lazily via require() on first activation and are cached per instance; prominence maps to size/opacity and 'hidden' renders no visible content while logic keeps running (MASC-04)"
    - "An unrecognized state prop clamps to idle instead of crashing (V5 input validation); a failed asset load falls back to the static surfaceElevated box with a dev-only console.warn (no user-facing error chrome)"
  artifacts:
    - path: "src/components/Mascot/Mascot.tsx"
      provides: "public <Mascot /> component — single LottieView owner, Reanimated fade, prominence sizing"
      contains: "LottieView"
      min_lines: 60
  key_links:
    - from: "src/components/Mascot/Mascot.tsx"
      to: "src/components/Mascot/useIdleScheduler.ts"
      via: "drives play() on the single LottieView ref"
      pattern: "useIdleScheduler"
    - from: "src/components/Mascot/Mascot.tsx"
      to: "assets/mascot/mascot_<state>.json"
      via: "lazy require() per state, cached in a ref map"
      pattern: "require\\(.*mascot_"
---

<objective>
Build the public `<Mascot />` component — the single persistent `LottieView` owner that renders all 5 states, drives idle micro-behaviors through the Plan 03 scheduler, fades transitions with Reanimated (120ms / 200ms reduced-stimulus), maps prominence to size/opacity, lazily loads + caches assets, and degrades safely (unknown state → idle; failed load → static box). Test-first: write the RED contract test, then implement to GREEN.

Purpose: This is the phase's vertical-slice core — the reusable, feature-agnostic module all later phases mount. It stitches Plans 01–03 together into a working component.
Output: `src/components/Mascot/Mascot.tsx` + `Mascot.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mascot-module/02-UI-SPEC.md
@.planning/phases/02-mascot-module/02-RESEARCH.md
@.planning/phases/02-mascot-module/02-PATTERNS.md

<interfaces>
From src/components/Mascot/types.ts (Plan 01): MascotState, MascotProminence, MascotSize, MascotProps
From src/components/Mascot/markers.ts (Plan 03): resolveMarkers, MarkerRange
From src/components/Mascot/useIdleScheduler.ts (Plan 03): useIdleScheduler, pickWeightedMicroBehavior, nextIdleIntervalMs
From src/components/Mascot/useReducedStimulus.ts (Plan 03): useReducedStimulus
Mock: __mocks__/lottie-react-native.tsx (testID "lottie-view-mock", play/pause/resume/reset jest.fn refs)
Size presets: lg=220, md=140, sm=64; prominence prominent->lg opacity 1.0, subtle->md opacity 0.85, hidden->0
Fade: 120ms normal, 200ms reduced-stimulus (Reanimated useAnimatedStyle opacity)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Mascot.test.tsx contract (RED)</name>
  <files>src/components/Mascot/__tests__/Mascot.test.tsx</files>
  <read_first>
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Validation Architecture → Test Map rows for MASC-01/03/04; Open Question 1 — prefer setTimeout-sized segment completion over onAnimationFinish)
    - src/app/__tests__/screens.test.tsx (RTL conventions — render() is async and MUST be awaited)
    - theme/__tests__/tokens.test.ts (render-under-ThemeProvider Probe pattern)
    - src/components/MascotSlot.tsx (fallback box treatment: surfaceElevated, radii.lg, StyleSheet.flatten never-array rule)
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (transition + asset/perf contract; prominence table)
  </read_first>
  <behavior>
    - Test 1 (MASC-01): rendering with each state prop passes the matching mascot_<state>.json object as the mocked LottieView `source` prop
    - Test 2 (MASC-04): rendering, then re-rendering through several state changes, yields exactly one `lottie-view-mock` node at all times (query the mock, assert single instance)
    - Test 3 (MASC-01): state='greeting' -> when the one-shot completes, onStateAnimationComplete('greeting') fires exactly once; same for 'acknowledge'
    - Test 4 (MASC-02 integration): in state='idle', advancing fake timers past the interval calls the mocked LottieView ref's play() with an idle marker frame range
    - Test 5 (MASC-04): prominence='hidden' renders no visible LottieView content (height 0 / not visibly mounted) while the component still accepts state changes without error
    - Test 6 (V5): an unknown state value passed via a cast renders the idle asset (clamp), not a crash
  </behavior>
  <action>
    Create `src/components/Mascot/__tests__/Mascot.test.tsx` (RED — precedes Mascot.tsx per the Phase 1 RED→GREEN precedent). Wrap renders in ThemeProvider and `await render(...)` (RTL v14 async). Rely on the auto-applied `__mocks__/lottie-react-native.tsx`. Use `jest.useFakeTimers()` for the idle-scheduler integration test. Assert the six behaviors above. For one-shot completion, drive completion the way the implementation will signal it (setTimeout-sized per Open Question 1, and/or the mock's onAnimationFinish) — keep the assertion on onStateAnimationComplete firing exactly once. Query single-instance via the mock's `testID="lottie-view-mock"`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Mascot.test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Mascot.test.tsx exists and encodes all six behaviors above
    - The suite runs and FAILS meaningfully before Mascot.tsx exists (RED proof) — failures reference missing component, not syntax errors
    - Tests wrap render in ThemeProvider and await it
  </acceptance_criteria>
  <done>A failing contract test pins MASC-01/03/04 + idle integration + degradation behavior before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mascot.tsx to GREEN</name>
  <files>src/components/Mascot/Mascot.tsx</files>
  <read_first>
    - src/components/Mascot/__tests__/Mascot.test.tsx (the contract just written — implement to satisfy it)
    - src/components/Mascot/types.ts, markers.ts, useIdleScheduler.ts, useReducedStimulus.ts (the contracts/consumed units)
    - src/components/MascotSlot.tsx (fallback box: surfaceElevated bg, border, radii.lg; StyleSheet.flatten([...]) never-array rule; testID + accessibilityLabel prop pattern)
    - .planning/phases/02-mascot-module/02-RESEARCH.md (Reanimated fade example useFadeOnStateChange; single-LottieView architecture diagram; lazy require + ref-map cache)
    - .planning/phases/02-mascot-module/02-UI-SPEC.md (transition contract, asset/perf contract, prominence table, Copywriting Contract — silent calm fallback, dev-only console.warn only)
  </read_first>
  <action>
    Create `src/components/Mascot/Mascot.tsx` implementing MascotProps (default state 'idle', prominence 'prominent'). Own exactly ONE `LottieView` via a ref; NEVER mount a second instance (no two-instance crossfade). Lazy-load each state's asset with `require('../../../assets/mascot/mascot_<state>.json')` at first activation, caching resolved sources in a `useRef` map keyed by state so re-entry does not re-require (validate/confirm the relative path from src/components/Mascot/ at edit time). Wrap the LottieView in an `Animated.View` whose `useAnimatedStyle` opacity fades 0→1 over 120ms (200ms when reduced-stimulus) on every state change (Reanimated, per RESEARCH example). Derive size from `size` prop else from prominence (prominent→lg 220 / opacity 1.0, subtle→md 140 / opacity 0.85, hidden→render a 0-height wrapper with no LottieView content but keep state/scheduler logic running). Use `useReducedStimulus(reducedStimulus)` and pass its value into `useIdleScheduler({ active: currentState === 'idle', reducedStimulus, markers: resolveMarkers(idleAsset), onPlay: range => lottieRef.current?.play(range.startFrame, range.endFrame) })`. For one-shot states (greeting, acknowledge) with loop=false, call `onStateAnimationComplete(state)` once on completion (setTimeout-sized to the asset op/fr per Open Question 1, or onAnimationFinish). Clamp an unrecognized `state` value to 'idle' before use (V5). On asset load/render failure, render the static `surfaceElevated` rounded box (MascotSlot treatment) and `console.warn` dev-only — never a toast/alert/error string. Apply `StyleSheet.flatten([...])` to any composed style prop (never pass a raw array). Pass through `testID` and `accessibilityLabel`. No `t()`/i18n imports inside this module; no hex literals in the .tsx (all colors via useTheme()). MASC-03: render only the 5 states — no code path constructs a negative/sad/waiting state.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Mascot.test && npm run lint:hex</automated>
  </verify>
  <acceptance_criteria>
    - All Mascot.test.tsx behaviors pass (GREEN)
    - Grep confirms exactly one `LottieView` element usage in Mascot.tsx (no second instance)
    - `require(` for state assets occurs lazily (inside a loader/callback keyed by state), not 5 eager top-level requires
    - Unknown state clamps to idle (Test 6 green)
    - `npm run lint:hex` passes (no hex literal in Mascot.tsx)
    - No import of react-i18next / i18n inside src/components/Mascot/
  </acceptance_criteria>
  <done><Mascot /> renders all 5 states on a single persistent LottieView with lazy-cached assets, Reanimated fade, prominence sizing, safe degradation, and idle micro-behaviors — full contract green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → module | Host passes state/prominence/size/accessibilityLabel props (TS-narrowed, but runtime-untrusted at the edge) |
| bundled asset → LottieView | Lazily required Lottie JSON rendered natively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Mascot `state` prop (V5 input validation) | mitigate | Clamp any unrecognized state value to 'idle' at runtime rather than trusting TS narrowing; unit-tested (Test 6). |
| T-02-01 | Denial of Service | eager asset decode / dual LottieView | mitigate | Lazy per-state require() cached per instance + single-persistent-LottieView contract (MASC-04), both asserted in tests. |
| T-02-06 | Information Disclosure | accessibilityLabel PII passthrough | accept | Host-supplied per Copywriting Contract; module adds a doc-comment noting the host must pass only i18next-translated, non-PII strings. |
</threat_model>

<verification>
- `npm test -- --testPathPattern=Mascot.test` green (GREEN after Task 2)
- `npm run lint:hex` passes for Mascot.tsx
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- Single persistent LottieView renders all 5 states with lazy-cached assets and a Reanimated fade
- Idle drives micro-behaviors; one-shots fire onStateAnimationComplete
- Unknown state and failed load both degrade safely; no negative state exists (MASC-03)
</success_criteria>

<output>
Create `.planning/phases/02-mascot-module/02-04-SUMMARY.md` when done
</output>
