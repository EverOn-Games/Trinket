---
phase: 01-scaffold-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.json
  - tsconfig.json
  - eas.json
  - .gitignore
  - babel.config.js
  - metro.config.js
  - app/_layout.tsx
  - app/index.tsx
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Repo contains a scaffolded Expo SDK 56 project with New Architecture, TypeScript strict, and Expo Router pre-wired"
    - "expo-dev-client, react-native-mmkv v4 + nitro-modules, zustand, i18next, react-i18next, expo-localization are installed at SDK-56-compatible versions"
    - "eas.json declares development/preview/production profiles and is JSON-valid without an EAS login"
    - "app.json declares name Trinket, slug trinket, scheme trinket, userInterfaceStyle dark, bundle/package com.everon.trinket, and expo-router + expo-localization plugins with no newArchEnabled key"
    - "expo prebuild generates ios/ and android/ config folders (native compile may fail in sandbox; config generation is the assertion)"
  artifacts:
    - path: "package.json"
      provides: "SDK 56 dependency graph with all native + i18n libraries"
      contains: "react-native-mmkv"
    - path: "app.json"
      provides: "App identity + dark lock + router/localization plugins"
      contains: "com.everon.trinket"
    - path: "eas.json"
      provides: "development/preview/production build profiles"
      contains: "developmentClient"
    - path: "tsconfig.json"
      provides: "strict TypeScript extending expo/tsconfig.base"
      contains: "\"strict\": true"
  key_links:
    - from: "app.json"
      to: "expo-router / expo-localization"
      via: "plugins array"
      pattern: "expo-router"
    - from: "package.json"
      to: "expo@56"
      via: "dependency pin"
      pattern: "\"expo\": \"[~^]?56"
---

<objective>
Scaffold the Trinket Expo SDK 56 project (Expo Router + TypeScript strict, New Architecture), install every native and cross-cutting dependency the project needs, and commit the build-configuration surface (app.json, eas.json, tsconfig strict) so the codebase is born CNG-first / dev-client-first — never Expo Go once MMKV lands (Pitfall 2/4).

Purpose: This is the habitat for every later phase. All other Phase 1 plans depend on this scaffold existing with the correct SDK, dependency versions, and build config.
Output: A working SDK 56 project at repo root with dependencies installed, app identity + build profiles configured, and native folders generated via prebuild.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-foundations/01-CONTEXT.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold SDK 56 project into repo root and install all dependencies</name>
  <files>package.json, package-lock.json, babel.config.js, metro.config.js, app/_layout.tsx, app/index.tsx</files>
  <read_first>
    - CLAUDE.md (Technology Stack — exact versions and "resolve via expo install" rule)
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (§Standard Stack, §Installation, Pattern A, Pitfall 2)
    - .planning/phases/01-scaffold-foundations/01-CONTEXT.md (D-05, D-06 app identity + local-workflow requirement)
  </read_first>
  <action>
The repo root currently contains only .git, .claude, .planning, and CLAUDE.md — create-expo-app refuses a non-empty target, so scaffold into a temp directory then move generated files into the repo root without touching .git, .claude, .planning, or CLAUDE.md.

Steps:
1. Run `npx create-expo-app@latest trinket-scaffold --template default@sdk-56 --yes` inside a scratch/temp location (e.g. the scratchpad dir), NOT the bare command (bare defaults to SDK 54 during the SDK 57 transition window — Pitfall 2). The explicit `--template default@sdk-56` is mandatory.
2. Move all generated files and folders (app/, package.json, package-lock.json, babel.config.js, metro.config.js, tsconfig.json, app.json, .gitignore, assets/, etc.) into /home/user/Trinket, preserving the existing .git, .claude, .planning, CLAUDE.md.
3. Verify the scaffold landed on SDK 56: `package.json` "expo" entry must resolve to the 56.x line (any 56.x.x patch is acceptable per Open Question 2). Assert with `npx expo config --type public` showing sdkVersion 56.x, or by reading package.json.
4. Install the dev-client + build tuning FIRST (before other native modules), then storage, i18n, and test/lint tooling, all via the SDK-56 resolver:
   - `npx expo install expo-dev-client expo-build-properties`
   - `npx expo install react-native-mmkv react-native-nitro-modules`
   - `npm install zustand`
   - `npm install i18next react-i18next`
   - `npx expo install expo-localization`
   - `npx expo install jest-expo jest @testing-library/react-native`
   - `npm install -D eslint-plugin-i18next`
5. Supply-chain guard (T-01-01/T-01-SC): after install, run `npm view react-native-mmkv scripts.postinstall` and `npm view react-native-nitro-modules scripts.postinstall`. Both are Approved in RESEARCH §Package Legitimacy Audit; if either shows an unexpected postinstall beyond standard Expo autolinking, STOP and surface it rather than proceeding.

Confirm the MMKV package resolves to mrousavy/react-native-mmkv (NOT the older ammarahm-ed react-native-mmkv-storage) per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && test -f package.json && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; const need=['expo','expo-router','expo-dev-client','react-native-mmkv','react-native-nitro-modules','zustand','i18next','react-i18next','expo-localization','jest-expo','eslint-plugin-i18next']; const miss=need.filter(k=>!d[k]); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(!/(^|[~^])56/.test(d.expo)){console.error('expo not 56:',d.expo);process.exit(1)} console.log('deps ok', d.expo)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` "expo" dependency matches `56.x` (regex `[~^]?56\.`)
    - `package.json` contains all of: expo-router, expo-dev-client, react-native-mmkv, react-native-nitro-modules, zustand, i18next, react-i18next, expo-localization, jest-expo, eslint-plugin-i18next
    - `.planning/`, `.git/`, `.claude/`, and `CLAUDE.md` remain intact at repo root after the move
    - `npm ls react-native-mmkv` shows the mrousavy package (not react-native-mmkv-storage)
    - postinstall audit output for react-native-mmkv and react-native-nitro-modules recorded in the SUMMARY (no unexpected scripts)
  </acceptance_criteria>
  <done>Scaffolded SDK 56 project sits at repo root with all native + i18n + tooling dependencies installed and the supply-chain postinstall check recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Configure app identity, strict TS, EAS profiles, and generate native folders</name>
  <files>app.json, tsconfig.json, eas.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (§Code Examples: app.json shape, eas.json shape; Anti-Patterns: no newArchEnabled; Open Question 1: prebuild on Linux)
    - .planning/phases/01-scaffold-foundations/01-CONTEXT.md (D-05 identity, D-06 EAS profiles + local-workflow)
    - app.json (scaffold default, to be edited)
    - tsconfig.json (scaffold default, to be edited)
  </read_first>
  <action>
Edit app.json under the "expo" key to set: name "Trinket", slug "trinket", scheme "trinket", userInterfaceStyle "dark", ios.bundleIdentifier "com.everon.trinket", ios.supportsTablet false, android.package "com.everon.trinket", and a plugins array containing "expo-router" and "expo-localization". Do NOT add a newArchEnabled key — it is a no-op on SDK 55+ and Expo docs recommend omitting it (Anti-Patterns / Pitfall 3), superseding CLAUDE.md's "pin it deliberately" wording. Leave D-05 identifiers as-is but note in the SUMMARY that com.everon.trinket is PENDING founder confirmation before any store upload.

Edit tsconfig.json to extend "expo/tsconfig.base" and set compilerOptions.strict true (the scaffold default already extends the base; ensure "strict": true is present explicitly).

Create eas.json with cli.version ">= 20.5.1", cli.appVersionSource "remote", and a build object with three profiles: development (developmentClient true, distribution "internal", android.buildType "apk", ios.simulator false), preview (distribution "internal", android.buildType "apk"), production (distribution "store"). This file must be JSON-valid without an EAS login (D-06) — do not run `eas login`.

Ensure .gitignore excludes the CNG-generated /ios and /android folders (they are build artifacts regenerated by prebuild), plus node_modules and .expo.

Then run `npx expo prebuild --clean` and `npx expo-doctor`. In this Linux sandbox, iOS pod install and full native compile WILL fail (no Xcode/Android SDK) — that is expected. The assertion is that config generation succeeds: android/ and ios/ folders are produced with app.json values applied. Record in the SUMMARY exactly what prebuild produced vs. what failed (per Open Question 1). Do NOT claim device compilation succeeded.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && node -e "const a=require('./app.json').expo; if(a.name!=='Trinket'||a.slug!=='trinket'||a.scheme!=='trinket'||a.userInterfaceStyle!=='dark')throw new Error('app.json identity wrong'); if(a.ios.bundleIdentifier!=='com.everon.trinket'||a.android.package!=='com.everon.trinket')throw new Error('bundle id wrong'); if(!a.plugins.includes('expo-router')||!a.plugins.includes('expo-localization'))throw new Error('plugins missing'); if('newArchEnabled' in a)throw new Error('newArchEnabled must be absent'); const e=require('./eas.json'); for(const p of ['development','preview','production'])if(!e.build[p])throw new Error('eas profile missing '+p); if(e.build.development.developmentClient!==true)throw new Error('dev profile missing developmentClient'); const t=require('./tsconfig.json'); if(t.compilerOptions.strict!==true)throw new Error('tsconfig not strict'); console.log('config ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.json` expo.name === "Trinket", slug === "trinket", scheme === "trinket", userInterfaceStyle === "dark"
    - `app.json` expo.ios.bundleIdentifier === "com.everon.trinket" and expo.android.package === "com.everon.trinket"
    - `app.json` expo.plugins includes "expo-router" and "expo-localization"; the `newArchEnabled` key is ABSENT
    - `tsconfig.json` compilerOptions.strict === true and extends "expo/tsconfig.base"
    - `eas.json` has development (with developmentClient: true), preview, and production build profiles and passes `node -e "require('./eas.json')"` (JSON-valid)
    - `.gitignore` excludes /ios, /android, node_modules, .expo
    - SUMMARY records the actual prebuild outcome (config generated vs. native-compile steps that failed in-sandbox)
  </acceptance_criteria>
  <done>App identity, strict TypeScript, and three EAS profiles are committed and JSON-valid; prebuild has generated native config folders with the outcome documented; expo-doctor run recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party native packages (MMKV, nitro-modules) execute install-time scripts and compile native code |
| app.json/eas.json → native binary | Build config governs what binary is produced and how it is distributed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | react-native-mmkv / react-native-nitro-modules install | mitigate | slopcheck-approved in RESEARCH §Package Legitimacy Audit (all 13 [OK]); Task 1 runs `npm view <pkg> scripts.postinstall` and stops on any unexpected postinstall beyond standard Expo autolinking |
| T-01-02 | Tampering | Wrong MMKV package (ammarahm-ed react-native-mmkv-storage) | mitigate | Task 1 asserts `npm ls react-native-mmkv` resolves to mrousavy package per CLAUDE.md |
| T-01-SC | Tampering | npm installs (supply chain) | mitigate | All packages pre-audited [OK]/Approved (no [ASSUMED]/[SUS]/[SLOP]); postinstall audit is the blocking guard |
| T-01-03 | Information Disclosure | eas.json distribution profiles | accept | Profiles are non-secret build config; no credentials stored in-repo (EAS login is user-side, D-06) |
</threat_model>

<verification>
- `node -e "require('./package.json')"`, `require('./app.json')`, `require('./eas.json')`, `require('./tsconfig.json')` all parse
- `npx expo-doctor` run and output recorded (warnings about missing native SDKs in sandbox are expected)
- SDK version confirmed 56.x
</verification>

<success_criteria>
- Scaffold present at repo root on SDK 56 with New Architecture (default, no explicit flag) and Expo Router pre-wired
- All native, state, i18n, and tooling dependencies installed at SDK-56-compatible versions
- app.json identity + dark lock + plugins correct; eas.json three profiles JSON-valid; tsconfig strict
- prebuild native-config generation attempted and its sandbox outcome documented (device compile deferred to user hardware)
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md` when done.
</output>
