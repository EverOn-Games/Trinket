---
phase: 01-scaffold-foundations
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - theme/tokens.ts
  - theme/ThemeProvider.tsx
  - theme/useTheme.ts
  - theme/index.ts
  - theme/__tests__/tokens.test.ts
  - scripts/check-hex-literals.mjs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "A single typed ThemeTokens object holds every visual value (colors, spacing, radii, typography scale, elevation) for the earthy night-cozy dark theme"
    - "Components consume tokens only through a useTheme() hook — never by importing tokens.ts directly and never via hex literals inline (D-02)"
    - "The token type shape does not hard-assume dark-only, so a light palette can be added later without a type refactor (deferred POLI-01)"
    - "A hex-literal scan proves no color literals exist outside theme/tokens.ts"
  artifacts:
    - path: "theme/tokens.ts"
      provides: "ThemeTokens type + darkTokens earthy palette"
      contains: "ThemeTokens"
    - path: "theme/ThemeProvider.tsx"
      provides: "React context provider supplying darkTokens"
      exports: ["ThemeProvider"]
    - path: "theme/useTheme.ts"
      provides: "useTheme() hook returning current tokens"
      exports: ["useTheme"]
    - path: "scripts/check-hex-literals.mjs"
      provides: "static gate: no #RRGGBB outside theme/"
      contains: "theme"
  key_links:
    - from: "theme/ThemeProvider.tsx"
      to: "theme/tokens.ts"
      via: "imports darkTokens"
      pattern: "darkTokens"
    - from: "theme/useTheme.ts"
      to: "theme/ThemeProvider.tsx"
      via: "useContext(ThemeContext)"
      pattern: "useContext"
---

<objective>
Build the theme token module (FND-04, D-01/D-02): a single typed `tokens.ts` holding the earthy, night-cozy, dark-only palette and scales, a custom `ThemeProvider`, and a `useTheme()` hook — the cross-cutting styling contract every screen consumes so the eventual Claude Design system swap is a one-file token replacement, not a component refactor.

Purpose: D-01 demands a genuine attempt at the "calm night-shift raccoon habitat" visual direction now, not a gray placeholder, because screens composed against a neutral scaffold would invite rework in every later phase. D-02 demands token-only styling enforced structurally.
Output: theme/ module (tokens, provider, hook, barrel) plus a hex-literal gate proving no color literals leak outside theme/.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-foundations/01-CONTEXT.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the earthy dark token module, provider, and hook</name>
  <files>theme/tokens.ts, theme/ThemeProvider.tsx, theme/useTheme.ts, theme/index.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (Pattern C — ThemeTokens type, darkTokens anchor values, custom provider rationale in Alternatives Considered)
    - .planning/phases/01-scaffold-foundations/01-CONTEXT.md (D-01, D-02, Claude's Discretion on token values; §specifics visual anchors)
  </read_first>
  <behavior>
    - useTheme() returns an object exposing colors, spacing, radii, typography, and elevation groups
    - darkTokens.colors.background, surface, textPrimary, accent are all valid hex strings
    - The ThemeTokens type is a named exported type (structure reusable for a future light palette without redefinition)
  </behavior>
  <action>
Create `theme/tokens.ts` exporting a `ThemeTokens` type with groups: colors (background, surface, surfaceElevated, textPrimary, textSecondary, accent, accentMuted, border), spacing (xs/sm/md/lg/xl as numbers), radii (sm/md/lg/pill), typography (fontFamily string + scale: caption/body/title/display numbers), elevation (none/low/medium numbers). Export a `darkTokens: ThemeTokens` value using the earthy night-cozy anchor palette from RESEARCH Pattern C (deep warm near-black background, warm-brown surfaces, cream text, warm-amber accent) — these are Claude's-discretion anchor values that swap when the design system lands; use the Pattern C values or refine toward "calm night-shift raccoon habitat" (soft rounded radii, generous spacing). The type must NOT bake in dark-only assumptions (no `dark`-prefixed keys in the type) so a `lightTokens` can be added later against the same shape (deferred POLI-01).

Create `theme/ThemeProvider.tsx`: a custom React context provider (NOT React Navigation's ThemeProvider — see RESEARCH Alternatives Considered) whose value is `darkTokens`, exporting `ThemeProvider` and the internal `ThemeContext`.

Create `theme/useTheme.ts` exporting `useTheme()` returning `useContext(ThemeContext)` typed as ThemeTokens.

Create `theme/index.ts` barrel re-exporting ThemeProvider, useTheme, ThemeTokens, and darkTokens.

Do NOT mount ThemeProvider into app/_layout.tsx here — that wiring belongs to Plan 06 (the app-shell integration plan) to keep this plan conflict-free with the i18n plan in the same wave.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx tsc --noEmit 2>&1 | tail -5 && npx jest theme --watchAll=false 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `theme/tokens.ts` exports a named type `ThemeTokens` and a `darkTokens` value with all groups: colors, spacing, radii, typography, elevation
    - `theme/ThemeProvider.tsx` exports `ThemeProvider`; `theme/useTheme.ts` exports `useTheme`
    - `npx tsc --noEmit` passes with strict mode (no type errors in theme/)
    - A test in `theme/__tests__/tokens.test.ts` (created in Task 2) asserting useTheme/darkTokens shape passes
    - The ThemeTokens type contains no `dark`-prefixed keys (light palette can reuse the shape)
  </acceptance_criteria>
  <done>theme/ module exists with a typed earthy dark token object, a custom provider, a useTheme hook, and a barrel export; strict TS compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Token shape test and the hex-literal gate</name>
  <files>theme/__tests__/tokens.test.ts, scripts/check-hex-literals.mjs</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (§Validation Architecture — FND-04 hex-scan row; D-02 enforcement)
    - theme/tokens.ts (the module under test, from Task 1)
  </read_first>
  <action>
Create `theme/__tests__/tokens.test.ts` asserting: darkTokens has all required color keys (background, surface, surfaceElevated, textPrimary, textSecondary, accent, accentMuted, border) each matching a hex-color regex; spacing/radii/typography.scale/elevation groups are present with numeric values; and useTheme (rendered under ThemeProvider, or the default context value) returns darkTokens.

Create `scripts/check-hex-literals.mjs`: a Node script that scans app/**/*.tsx, features/**/*.tsx, and components/**/*.tsx (whatever exists) for hex color literals matching `#[0-9a-fA-F]{3,8}` and exits non-zero if any are found OUTSIDE theme/ — this is the D-02 structural guard that color values live only in theme/tokens.ts. Strip commented lines before matching (use `grep -v '^\s*//'`-equivalent logic or a comment-aware scan) so header comments do not self-invalidate the gate. At this stage the only source files are scaffold defaults + theme/; the gate should pass (or list scaffold violations that Plan 06 will fix when it replaces screens). Wire the script as an npm script `lint:hex` in package.json.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx jest theme --watchAll=false 2>&1 | tail -10 && node scripts/check-hex-literals.mjs; echo "hex gate exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `theme/__tests__/tokens.test.ts` passes: asserts all color keys match a hex regex and all numeric scale groups exist
    - `scripts/check-hex-literals.mjs` exits 0 when no hex literals exist outside theme/, and non-zero when they do (verify by a temporary probe if needed)
    - The script excludes comment lines from matching (comment-aware)
    - `package.json` scripts includes a `lint:hex` entry invoking the script
  </acceptance_criteria>
  <done>Token shape test passes and the hex-literal gate enforces color-values-only-in-theme, wired as an npm script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Theme tokens are static in-app constants; no external input, network, or storage crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Style drift (hex literals bypassing tokens) | mitigate | scripts/check-hex-literals.mjs gate + eslint keep color values confined to theme/tokens.ts (D-02) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx jest theme --watchAll=false` passes
- `node scripts/check-hex-literals.mjs` exits 0
</verification>

<success_criteria>
- A genuine earthy, night-cozy, dark-only token module exists (not a gray placeholder — D-01)
- Every visual value lives in theme/tokens.ts; consumption is via useTheme() only (D-02), enforced by the hex gate
- The token type shape is light-mode-ready without a future refactor
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-03-SUMMARY.md` when done.
</output>
