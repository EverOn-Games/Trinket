---
phase: 01-scaffold-foundations
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/co-pilot.tsx
  - app/brain-dump.tsx
  - app/starter.tsx
  - app/history.tsx
  - app/settings.tsx
  - components/Screen.tsx
  - components/MascotSlot.tsx
  - app/__tests__/screens.test.tsx
autonomous: false
requirements: [FND-01, FND-04, FND-05]
must_haves:
  truths:
    - "The root layout mounts ThemeProvider and initializes i18n before any screen renders, and persists the resolved locale to the settings store"
    - "A home-hub screen (not a tab bar) presents a mascot placeholder slot, a primary 'Start a session?' offer, prominent Brain dump access, and links to Starter, History, and Settings (D-03)"
    - "Six placeholder screens (home, co-pilot, brain-dump, starter, history, settings) render themed, localized shells with zero hardcoded strings and zero hex literals outside theme/"
    - "Brain dump is reachable directly from home (<=2 taps from anywhere, DUMP-05 constraint on the skeleton)"
    - "The walking-skeleton slice works end-to-end: tapping the home offer writes a real session record via sessionsRepo, and the History screen lists that persisted session"
    - "The app boots on a physical iOS and Android device via EAS dev-client (verified on user hardware — machine-unverifiable in the sandbox)"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "root providers: ThemeProvider + i18n init + settings-locale persistence"
      contains: "ThemeProvider"
    - path: "app/index.tsx"
      provides: "home-hub screen with Start-a-session offer + navigation"
      contains: "useTheme"
    - path: "components/MascotSlot.tsx"
      provides: "explicit mount point for the Phase 2 mascot module"
      contains: "MascotSlot"
  key_links:
    - from: "app/index.tsx"
      to: "data/repositories/sessions.ts"
      via: "sessionsRepo.create on offer tap"
      pattern: "sessionsRepo"
    - from: "app/history.tsx"
      to: "data/repositories/sessions.ts"
      via: "sessionsRepo.list"
      pattern: "sessionsRepo"
    - from: "app/_layout.tsx"
      to: "theme + i18n"
      via: "provider mount + init"
      pattern: "ThemeProvider|i18n"
---

<objective>
Wire the cross-cutting providers and ship the Walking Skeleton slice: a home-hub app shell (D-03) with six themed, localized placeholder screens, where tapping the home "Start a session?" offer writes a real session to MMKV via sessionsRepo and the History screen reads it back — proving scaffold + routing + theme + i18n + MMKV persistence + one real UI interaction end-to-end. Then verify the one criterion the sandbox cannot: real-device boot via dev-client (human checkpoint).

Purpose: This is the vertical slice that turns Phase 1's infrastructure into a booting, navigable, persisting app — the habitat the mascot (Phase 2) and the core loop (Phase 3+) mount into. After this plan, a real user can open the app, move between all home-hub destinations, and cause a real persisted write.
Output: app/_layout.tsx providers, six routed screen shells, shared Screen + MascotSlot components, a screen render test, and a signed-off device-boot verification.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-foundations/01-CONTEXT.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md
@.planning/phases/01-scaffold-foundations/01-03-SUMMARY.md
@.planning/phases/01-scaffold-foundations/01-04-SUMMARY.md
@.planning/phases/01-scaffold-foundations/01-05-SUMMARY.md
<interfaces>
From theme/ (Plan 03): `ThemeProvider` (component), `useTheme(): ThemeTokens`, `darkTokens`, type `ThemeTokens` (colors/spacing/radii/typography/elevation).
From i18n/ (Plan 04): default i18n instance (already .init'd on import), `resolveInitialLocale()`, `useLocale()` -> { locale, setLocale }; use react-i18next `useTranslation()` -> `t` in screens.
From data/ (Plan 05): `sessionsRepo` (create/get/list/update/remove), `dumpItemsRepo`, `intentionsRepo`, `useSettingsStore` (locale, notificationsOptIn, setLocale, setNotificationsOptIn).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount root providers and build the home-hub + five placeholder screens</name>
  <files>app/_layout.tsx, app/index.tsx, app/co-pilot.tsx, app/brain-dump.tsx, app/starter.tsx, app/history.tsx, app/settings.tsx, components/Screen.tsx, components/MascotSlot.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-CONTEXT.md (D-03 home-hub layout, D-04 route set, D-07/D-08 locale, §specifics offer-not-instruct copy grammar)
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (System Architecture Diagram — _layout mounts ThemeProvider then i18n gate; Recommended Project Structure)
    - theme/index.ts, i18n/index.ts, i18n/useLocale.ts, data/stores/useSettingsStore.ts (the interfaces this task consumes)
    - app/_layout.tsx and app/index.tsx (scaffold defaults, to be replaced)
  </read_first>
  <action>
Replace the scaffold's app/_layout.tsx with a root layout that: imports the i18n instance (side-effect init on import, from i18n/index.ts), wraps the Expo Router Stack in the custom `ThemeProvider` (from theme/, NOT React Navigation's), and on mount persists the resolved initial locale into useSettingsStore (call setLocale with resolveInitialLocale()'s value if the store has no persisted locale yet) so D-07's device resolution is durably stored. Configure the Stack so routes are the home-hub set, not tabs (D-03).

Create `components/Screen.tsx`: a themed screen container consuming useTheme() for background/padding, giving every screen the dark earthy surface with safe-area handling (use react-native-safe-area-context which ships with the template). All screens render inside it.

Create `components/MascotSlot.tsx`: an explicit placeholder box (themed, sized) that reserves the mascot mount point on the home screen — Phase 2 replaces its innards with the real Lottie mascot module. It must be a clearly named seam (the integration point noted in CONTEXT §code_context).

Build the six routes as themed, localized shells (each uses useTheme() + useTranslation()'s t() — NO hex literals, NO raw JSX strings):
- app/index.tsx (home-hub, D-03): renders MascotSlot at top, a primary "Start a session?" offer control (offer grammar — a button that offers, never a mascot command), a prominent secondary Brain dump entry, and navigation affordances to Starter, History, and Settings. Brain dump must be reachable directly from home (DUMP-05: <=2 taps from anywhere).
- app/co-pilot.tsx: session-flow stub shell (title + placeholder copy, no session logic — that is Phase 3).
- app/brain-dump.tsx, app/starter.tsx, app/history.tsx, app/settings.tsx: titled localized shells with a short offer-grammar description each, no feature logic.

All copy comes from the i18n keys authored in Plan 04 (add any missing keys to both en.json and pl.json). Navigation uses expo-router Link/router. Do not add feature behavior beyond navigation and the skeleton write wired in Task 2.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx tsc --noEmit 2>&1 | tail -5 && npx eslint app components --max-warnings=0 2>&1 | tail -15 && node scripts/check-hex-literals.mjs; echo "hex exit $?"</automated>
  </verify>
  <acceptance_criteria>
    - `app/_layout.tsx` wraps the router in `ThemeProvider` and imports the i18n instance; on mount it persists resolveInitialLocale() to useSettingsStore when no locale is stored
    - Six route files exist: app/index.tsx, co-pilot.tsx, brain-dump.tsx, starter.tsx, history.tsx, settings.tsx
    - app/index.tsx renders MascotSlot, a "Start a session?" offer, and a direct Brain dump entry (reachable from home = <=2 taps)
    - `npx eslint app components --max-warnings=0` passes (zero i18next/no-literal-string violations — proves no hardcoded copy)
    - `node scripts/check-hex-literals.mjs` exits 0 (no color literals outside theme/)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Root providers are mounted, the resolved locale persists to settings, and all six home-hub screens render as themed, fully-localized shells with zero hardcoded copy and zero stray hex literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Walking-skeleton end-to-end slice (home offer writes a session, History reads it)</name>
  <files>app/index.tsx, app/history.tsx, app/__tests__/screens.test.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (System Architecture Diagram — the one real repository read/write proving storage end-to-end)
    - data/repositories/sessions.ts (sessionsRepo contract from Plan 05)
    - app/index.tsx, app/history.tsx (from Task 1)
  </read_first>
  <behavior>
    - Tapping the home "Start a session?" offer calls sessionsRepo.create and the new session is persisted (retrievable via sessionsRepo.list)
    - The History screen renders each persisted session from sessionsRepo.list as a plain chronological entry — no statistics, no completion rate, no daily boundaries (PILOT-07 spirit)
    - Rendering the home screen and History screen under the providers does not throw
  </behavior>
  <action>
Wire the walking-skeleton slice: in app/index.tsx, make the "Start a session?" offer's onPress call `sessionsRepo.create(...)` with a minimal valid session record (a start timestamp; this is the thinnest real write — full session lifecycle is Phase 3) and then navigate to (or surface) confirmation. In app/history.tsx, read `sessionsRepo.list()` and render the sessions as a quiet chronological list (localized empty-state copy when none) — plain log only, no stats/rates/day-grouping.

Create `app/__tests__/screens.test.tsx` using @testing-library/react-native (against the mocked MMKV): render the home screen inside ThemeProvider, simulate the offer press, assert sessionsRepo.list() now contains one session; render the History screen and assert it shows that session; assert the empty History state renders localized copy when no sessions exist. This is the end-to-end proof of the skeleton write/read path at the JS layer (the native persistence guarantee is confirmed on-device in Task 3).
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx jest app --watchAll=false 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Pressing the home offer in the test creates exactly one session via sessionsRepo (sessionsRepo.list length increments)
    - History screen test renders the created session and shows localized empty-state copy when none exist
    - History renders a plain chronological list with no statistics/completion-rate/daily-boundary UI
    - `npx jest app --watchAll=false` exits 0
  </acceptance_criteria>
  <done>The walking-skeleton vertical slice works: a real UI interaction persists a real session to MMKV and the History screen reads it back, proven by an automated render+interaction test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device-boot verification via EAS dev-client (success criterion 1)</name>
  <files>N/A — human verification on user hardware; no repo files change</files>
  <action>
This task is NOT machine-executable in the sandbox (no Xcode, no Android SDK, no devices). The executor pauses here and hands off to the user for physical-device verification. Do not mark success criterion 1 (real-device boot) as satisfied from sandbox checks alone — it is explicitly a human checkpoint. Present the what-built / how-to-verify guidance below and wait for the resume signal.
  </action>
  <what-built>
A complete SDK 56 Walking Skeleton: dev-client-compatible native config (Plan 01), themed + localized home-hub with six screens (Task 1), and a real MMKV persistence round-trip on the home + History screens (Task 2). Everything machine-verifiable in the Linux sandbox is green (tsc strict, jest, eslint no-literal-string, hex gate, expo-doctor). The one criterion the sandbox CANNOT verify — actual boot on physical hardware — requires your machine or an EAS cloud build.
  </what-built>
  <how-to-verify>
On your own hardware (the sandbox has no Xcode, no Android SDK, no devices):
1. `eas login` (D-06 — cloud builds need your Expo account; local runs do not).
2. Android: either `npx expo run:android` with Android Studio/SDK installed, OR `eas build --profile development --platform android` and install the resulting APK on a physical Android device.
3. iOS: `eas build --profile development --platform ios` (no Linux/Windows alternative exists for iOS) and install on a physical iPhone via the dev-client.
4. Launch the dev-client build on BOTH a physical iOS device and a physical Android device (NOT Expo Go — MMKV hard-fails there).
5. Confirm: the app boots to the dark earthy home-hub; copy renders in your device language (Polish device -> Polish, else English); tapping "Start a session?" then opening History shows the session persisted; navigate to Brain dump directly from home.
6. Confirm the bundle identifier com.everon.trinket with the founder before any TestFlight/Play upload (D-05 is PENDING confirmation).
  </how-to-verify>
  <verify>
    <human-check>App boots to the themed home-hub and the home->write->History flow works on both a physical iOS and a physical Android device via dev-client (not Expo Go).</human-check>
  </verify>
  <resume-signal>Type "approved" once the app has booted and the home->write->History flow works on both a physical iOS and a physical Android device, or describe what failed.</resume-signal>
  <done>User has confirmed the dev-client build boots and the walking-skeleton flow works on both a physical iOS and a physical Android device (success criterion 1 signed off).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI interaction → local MMKV store | The home offer triggers a real persisted write; the only user-driven data flow in Phase 1 |
| dev-client distribution | Development builds are internal-distribution only (eas.json) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Repudiation | Session write with no audit trail | accept | Single-user local app; sessions are the user's own quiet log (PILOT-07), no audit requirement |
| T-01-12 | Tampering | Malicious/oversized copy injection via locale files | mitigate | Copy is developer-authored JSON, run through eslint + reviewable for forbidden medical/shame language; no user-supplied strings in Phase 1 |
| T-01-06 | Tampering | Hardcoded copy in screens (regulatory/shame-free) | mitigate | eslint i18next/no-literal-string with --max-warnings=0 blocks the plan if any screen has raw JSX text |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npx jest --watchAll=false` (full suite), `npx eslint . --max-warnings=0`, `node scripts/check-hex-literals.mjs`, `npx expo-doctor` all green in-sandbox
- Device-boot on physical iOS + Android via dev-client signed off by the user (Task 3 checkpoint)
</verification>

<success_criteria>
- App shell is a home-hub (D-03) with six themed, localized screens and Brain dump reachable directly from home (DUMP-05)
- The walking-skeleton slice persists a real session via a real UI interaction and reads it back on History (end-to-end stack proven at the JS layer)
- Real-device boot via dev-client verified on the user's hardware (success criterion 1) — explicitly signed off, not silently claimed
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-06-SUMMARY.md` when done.
</output>
