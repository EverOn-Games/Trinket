---
phase: 01-scaffold-foundations
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - jest.config.js
  - jest.setup.ts
  - __mocks__/react-native-mmkv.ts
  - eslint.config.js
autonomous: true
requirements: [FND-01, FND-02, FND-05]
must_haves:
  truths:
    - "Jest runs under the jest-expo preset with react-native-mmkv mocked by an in-memory fake, so repository logic is unit-testable without the native binding (Pitfall 1)"
    - "ESLint runs the eslint-plugin-i18next no-literal-string flat-config rule so hardcoded JSX copy is flagged mechanically (FND-05 enforcement)"
    - "A trivial passing smoke test proves the jest harness executes green in the sandbox"
  artifacts:
    - path: "jest.config.js"
      provides: "jest-expo preset + setup file wiring"
      contains: "jest-expo"
    - path: "__mocks__/react-native-mmkv.ts"
      provides: "in-memory createMMKV/set/getString/remove fake"
      contains: "createMMKV"
    - path: "eslint.config.js"
      provides: "eslint-config-expo + eslint-plugin-i18next flat config"
      contains: "i18next"
  key_links:
    - from: "jest.config.js"
      to: "__mocks__/react-native-mmkv.ts"
      via: "moduleNameMapper or automock setup"
      pattern: "react-native-mmkv"
    - from: "eslint.config.js"
      to: "eslint-plugin-i18next"
      via: "flat config plugin import"
      pattern: "i18next"
---

<objective>
Stand up the Wave 0 test and lint harness that every later Phase 1 plan verifies against: a jest-expo Jest config with an in-memory react-native-mmkv mock (MMKV v4 cannot initialize Nitro Modules under Node — Pitfall 1), and a flat ESLint config wiring eslint-plugin-i18next's no-literal-string rule (the mechanical enforcement for FND-05's zero-hardcoded-copy requirement).

Purpose: Repository CRUD tests, i18n plural/locale tests, and the hardcoded-string gate all need this harness to exist before they can run. Establishing it once here avoids Wave-3 file conflicts across the theme/i18n/repository plans.
Output: `jest.config.js`, `jest.setup.ts`, `__mocks__/react-native-mmkv.ts`, and `eslint.config.js`, all executing green.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jest harness with in-memory MMKV mock and passing smoke test</name>
  <files>jest.config.js, jest.setup.ts, __mocks__/react-native-mmkv.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (Pitfall 1; §Validation Architecture — Test Framework, Wave 0 Gaps; Pattern B for the v4 API shape)
    - package.json (confirm jest-expo and jest are installed from Plan 01)
  </read_first>
  <action>
Create `jest.config.js` exporting a config with preset "jest-expo", setupFilesAfterEnv pointing at "./jest.setup.ts", and a transformIgnorePatterns that permits react-native / expo / @react-native community packages to transform (jest-expo's default pattern is acceptable — keep it).

Create `__mocks__/react-native-mmkv.ts` implementing an in-memory fake of the MMKV v4 surface used by the repositories: an exported `createMMKV({ id })` factory returning an object backed by a per-instance `Map<string,string>` with methods `set(key, value)`, `getString(key)` (returns the stored string or undefined), `getBoolean`, `getNumber`, `remove(key)` (alias `delete` if the real API uses it — mirror whatever `data/mmkv.ts` will call, which is `.set`/`.getString`/`.remove` per RESEARCH Pattern B), `contains(key)`, `getAllKeys()`, and `clearAll()`. Distinct `id` values must yield independent Maps so contentStorage and settingsStorage do not collide. This mock is the ONLY way repository logic gets tested (Pitfall 1: the real native binding throws "Failed to get NitroModules" under Node).

In `jest.setup.ts`, register the mock (e.g. `jest.mock('react-native-mmkv')` so Jest resolves the `__mocks__` manual mock) and add any global test setup. Add a trivial smoke test file (e.g. `__tests__/harness.smoke.test.ts`) asserting the mock round-trips: createMMKV, set a key, getString returns it, remove clears it — proving the harness runs green.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx jest --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `jest.config.js` sets preset "jest-expo" and setupFilesAfterEnv includes "./jest.setup.ts"
    - `__mocks__/react-native-mmkv.ts` exports `createMMKV` returning an object with working `set`/`getString`/`remove` backed by an in-memory Map
    - Two `createMMKV` instances with different `id` do not share state (independent Maps)
    - `npx jest --watchAll=false` exits 0 with the smoke test passing (no "Failed to get NitroModules" error)
  </acceptance_criteria>
  <done>Jest runs green under jest-expo with react-native-mmkv fully mocked in-memory and a smoke test proving the round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Flat ESLint config with eslint-plugin-i18next no-literal-string rule</name>
  <files>eslint.config.js</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (§Standard Stack: eslint-config-expo + eslint-plugin-i18next; §Don't Hand-Roll: hardcoded-string enforcement)
    - eslint.config.js (scaffold default if the template shipped one; otherwise create)
  </read_first>
  <action>
Create or extend `eslint.config.js` as a flat config that composes eslint-config-expo's flat config with eslint-plugin-i18next's `configs['flat/recommended']` (which enables the `no-literal-string` rule). Scope the no-literal-string rule to JSX-bearing source (app/**, and later features/**) so it flags hardcoded user-facing copy but does not fire on config files, test files, mock files, theme token values, or the i18n locale JSON. Add sensible ignores for node_modules, ios/, android/, .expo/, __mocks__/, and *.test.ts / __tests__/.

The intent: any raw JSX text string in a screen (e.g. a literal "Start a session?" in <Text>) is an ESLint error, forcing use of `t()` translation keys (FND-05). Verify the rule is active by running eslint against the current app/ tree; the scaffold's default screens may contain literal strings — if so, that is expected and will be resolved when Plan 06 replaces those screens with localized shells. For this task, assert the rule LOADS and RUNS (eslint exits without a config error); do not require zero violations yet, since screen localization lands in Plan 06.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx eslint --print-config app/index.tsx 2>&1 | grep -q "i18next/no-literal-string" && echo "no-literal-string rule active" || (echo "rule not wired"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` imports eslint-plugin-i18next and composes eslint-config-expo in flat-config form
    - `npx eslint --print-config app/index.tsx` output includes the rule key `i18next/no-literal-string`
    - The config ignores ios/, android/, node_modules/, .expo/, __mocks__/, and test files
    - `npx eslint .` runs without a configuration/parse error (rule violations in scaffold-default screens are acceptable at this stage)
  </acceptance_criteria>
  <done>ESLint flat config runs the i18next/no-literal-string rule over app source, wired and confirmed active via --print-config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → native module | Jest (Node) cannot load Nitro-backed MMKV; a mock substitutes at the module boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Test mock masking real behavior | accept | Mock covers logic only; on-device persistence (success criterion 1/2 real store) is verified separately on user hardware, documented not silently claimed |
| T-01-SC | Tampering | eslint-plugin-i18next (dev dependency) | mitigate | Approved [OK] in RESEARCH §Package Legitimacy Audit; dev-only, not shipped in the app binary |
</threat_model>

<verification>
- `npx jest --watchAll=false` exits 0
- `npx eslint --print-config app/index.tsx` shows the no-literal-string rule
</verification>

<success_criteria>
- Jest harness runs green with the in-memory MMKV mock (no NitroModules error)
- ESLint flat config actively enforces i18next/no-literal-string over app source
- Both harnesses are ready for the theme, i18n, and repository plans in Wave 3 to verify against
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-02-SUMMARY.md` when done.
</output>
