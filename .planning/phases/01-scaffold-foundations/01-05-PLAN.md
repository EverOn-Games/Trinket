---
phase: 01-scaffold-foundations
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - data/mmkv.ts
  - data/repositories/sessions.ts
  - data/repositories/dumpItems.ts
  - data/repositories/intentions.ts
  - data/repositories/settings.ts
  - data/stores/useSettingsStore.ts
  - data/types.ts
  - lib/id.ts
  - data/repositories/__tests__/repositories.test.ts
  - data/repositories/__tests__/schema.denylist.test.ts
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "sessions, dumpItems, and intentions each have a working MMKV-backed repository with create/get/list/update/remove using per-record keys plus an index key"
    - "settings persists through a Zustand persist store backed by an MMKV StateStorage adapter (single-blob singleton)"
    - "No streak, daily-aggregate, or diagnosis-status field name exists anywhere in the four schemas — enforced by a denylist test that also guards later phases"
    - "Repository logic is unit-tested against the in-memory MMKV mock (real native persistence is device-only, per Pitfall 1)"
  artifacts:
    - path: "data/mmkv.ts"
      provides: "createMMKV instance factory (contentStorage, settingsStorage)"
      contains: "createMMKV"
    - path: "data/repositories/sessions.ts"
      provides: "sessions CRUD repository"
      exports: ["sessionsRepo"]
    - path: "data/stores/useSettingsStore.ts"
      provides: "Zustand persist settings singleton over MMKV"
      contains: "persist"
    - path: "data/repositories/__tests__/schema.denylist.test.ts"
      provides: "structural guard against streak/daily/diagnosis fields"
      contains: "streak"
  key_links:
    - from: "data/repositories/sessions.ts"
      to: "data/mmkv.ts contentStorage"
      via: "createMMKV instance"
      pattern: "contentStorage"
    - from: "data/stores/useSettingsStore.ts"
      to: "data/mmkv.ts settingsStorage"
      via: "StateStorage adapter"
      pattern: "settingsStorage"
---

<objective>
Build the MMKV persistence layer (FND-02): a `createMMKV()` instance factory (v4 API, NOT `new MMKV()`), typed repositories for the three growing collections (sessions, dumpItems, intentions) using per-record keys + an index key, and a Zustand-persist settings singleton backed by an MMKV StateStorage adapter — with a structural denylist test guaranteeing no streak/daily-aggregate/diagnosis field ever enters the schema.

Purpose: Success criterion 2 requires all four collections persist locally with zero pressure-oriented fields. "If a stat can only be used for pressure, it does not exist in the schema" is data-model dogma. The denylist test makes that dogma mechanically enforced across every future phase, not just this one.
Output: data/ layer (mmkv factory, four repositories, settings store, shared types) + repository CRUD tests + schema denylist test.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-foundations/01-CONTEXT.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md
@.planning/source/trinket-dev-synthesis-v0.1.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MMKV factory, shared types, and the three collection repositories</name>
  <files>data/mmkv.ts, data/types.ts, data/repositories/sessions.ts, data/repositories/dumpItems.ts, data/repositories/intentions.ts, lib/id.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (Pattern B — createMMKV v4 API, per-record + index-key repository shape; Anti-Patterns: not new MMKV(); Pitfall 1)
    - .planning/source/trinket-dev-synthesis-v0.1.md (§6.2 data model sketch — exact field names for dump_items, intentions, sessions, settings; schema field names MUST match this)
    - __mocks__/react-native-mmkv.ts (the mock these repos are tested against, from Plan 02)
    - .planning/research/ARCHITECTURE.md (Pattern 2 — repository-over-MMKV layering, sessionsRepo reference)
  </read_first>
  <behavior>
    - sessionsRepo.create(input) returns a record with a generated id and persists it; get(id) returns it; list() includes it; update(id, patch) mutates it; remove(id) deletes it and drops it from list()
    - dumpItemsRepo and intentionsRepo expose the identical CRUD contract over their own record types
    - Two repositories writing to the same contentStorage instance do not collide (namespaced keys)
    - No record type includes any pressure/aggregate field (verified in Task 2)
  </behavior>
  <action>
Create `lib/id.ts` exporting a `newId()` using `crypto.randomUUID()` (available in RN 0.85 / Hermes; if unavailable in the mock/test env, fall back to a small uuid-v4 generator) — do not add a uuid dependency.

Create `data/mmkv.ts`: import `createMMKV` from 'react-native-mmkv' and export `contentStorage = createMMKV({ id: 'trinket-content' })` and `settingsStorage = createMMKV({ id: 'trinket-settings' })`. Use `createMMKV`, never `new MMKV()` (v4 breaking change). Encryption is Claude's-discretion / deferred to Phase 7 per RESEARCH Security Domain V6 — do NOT add an encryptionKey now; document the deferral in the SUMMARY (T-01-08).

Create `data/types.ts`: define the record types for Session, DumpItem, Intention using the field names from source synthesis §6.2. Every collection record carries an `id: string` and a creation timestamp (an absolute epoch/ISO field — sessions specifically use persisted start/end timestamps so elapsed time is derived, per PILOT-06's later needs, NOT a duration counter). CRITICAL: include NO field named or resembling streak, dailyCount, dailyTotal, completionRate, dayChain, lastActiveDate, diagnosis, adhdStatus, or any daily-boundary aggregate. There are no daily boundaries — a session crossing midnight is unremarkable.

Create `data/repositories/sessions.ts`, `dumpItems.ts`, `intentions.ts`: each exports a repo object (`sessionsRepo`, `dumpItemsRepo`, `intentionsRepo`) implementing `create(input): Record`, `get(id): Record | undefined`, `list(): Record[]`, `update(id, patch): Record | undefined`, `remove(id): void`, following ARCHITECTURE.md Pattern 2 — store each record as JSON under a namespaced per-record key (e.g. `session:<id>`) in contentStorage, and maintain a JSON index array under a collection index key (e.g. `session:index`) so list() is O(n) without a full keyscan. create() uses newId() and sets the creation timestamp; update() merges a patch; remove() deletes the record key and prunes the index. Keep namespaces distinct per collection so they share contentStorage without collision.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx tsc --noEmit 2>&1 | tail -5 && npx jest data/repositories --watchAll=false 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `data/mmkv.ts` uses `createMMKV({ id })` (grep confirms no `new MMKV(`)
    - `sessionsRepo`, `dumpItemsRepo`, `intentionsRepo` each export create/get/list/update/remove
    - `data/repositories/__tests__/repositories.test.ts` (Task 2) round-trips each repo through the in-memory mock and passes
    - Two repos writing to contentStorage do not overwrite each other's records (namespaced keys)
    - `npx tsc --noEmit` passes under strict mode
    - Field names in data/types.ts match source synthesis §6.2
  </acceptance_criteria>
  <done>MMKV v4 factory and three collection repositories exist with full CRUD over per-record + index keys, compiling under strict TS and round-tripping through the mock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings Zustand-persist store, repository CRUD tests, and schema denylist guard</name>
  <files>data/repositories/settings.ts, data/stores/useSettingsStore.ts, data/repositories/__tests__/repositories.test.ts, data/repositories/__tests__/schema.denylist.test.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (Pattern B — Zustand StateStorage adapter from MMKV docs; §Validation Architecture FND-02 rows; Security Domain — diagnosis-field denylist extension)
    - data/mmkv.ts and data/types.ts (from Task 1)
    - .planning/source/trinket-dev-synthesis-v0.1.md (§6.2 settings fields)
  </read_first>
  <behavior>
    - useSettingsStore persists locale, notificationsOptIn, subscriptionCache to settingsStorage and rehydrates them
    - settingsRepo (or the store) exposes reading/updating locale + notification opt-in
    - The denylist test fails if any of streak/dailyCount/dailyTotal/completionRate/dayChain/lastActiveDate/diagnosis/adhdStatus appear as a key in any of the four schemas
  </behavior>
  <action>
Create `data/stores/useSettingsStore.ts`: a Zustand store using `persist` with a `StateStorage` adapter over settingsStorage (setItem -> settingsStorage.set, getItem -> settingsStorage.getString(name) ?? null, removeItem -> settingsStorage.remove) exactly per RESEARCH Pattern B / MMKV's WRAPPER_ZUSTAND_PERSIST_MIDDLEWARE doc. State shape: `locale: 'pl' | 'en'`, `notificationsOptIn: boolean`, `subscriptionCache: unknown` (typed placeholder populated in Phase 7), plus setters `setLocale`, `setNotificationsOptIn`. Default locale 'en' (i18n resolves the real initial value at boot and writes it here). This is the settings singleton — do NOT give settings the collection repository treatment.

Create `data/repositories/settings.ts` as a thin typed accessor over the settings store (or a direct settingsStorage wrapper) exposing get/set of the settings blob, so callers have a repository-shaped seam consistent with the other three.

Create `data/repositories/__tests__/repositories.test.ts`: exercise each of sessionsRepo/dumpItemsRepo/intentionsRepo through the full CRUD lifecycle against the mock (create -> get -> list contains -> update reflects -> remove -> list excludes), plus a cross-collection isolation assertion. Include a settings persistence round-trip (set locale/notif, re-read).

Create `data/repositories/__tests__/schema.denylist.test.ts`: assemble the set of all field names across Session, DumpItem, Intention, and the settings state (introspect a representative created record of each type's keys), and assert the intersection with the denylist [streak, streakCount, dailyCount, dailyTotal, dailyAggregate, completionRate, dayChain, lastActiveDate, activeDays, diagnosis, adhdStatus, diagnosisStatus] is EMPTY. Use a comment-stripped, explicit key-set check (not a raw grep with `== 0` on a whole file). This guard also catches accidental additions in later phases.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx jest data --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `useSettingsStore` uses zustand persist with a StateStorage adapter over settingsStorage and exposes setLocale + setNotificationsOptIn
    - repositories.test.ts passes: full CRUD lifecycle for all three collections + cross-collection isolation + settings round-trip
    - schema.denylist.test.ts passes and FAILS if a denied field is added (verify by a temporary probe: adding `streak` to a type makes it red)
    - `npx jest data --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Settings persist through Zustand+MMKV, all repositories pass CRUD tests, and the schema denylist guard structurally forbids streak/daily-aggregate/diagnosis fields across all four schemas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime → local MMKV store | User content is written to and read from on-device storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Unencrypted MMKV content on a compromised/physical-access device | accept (deferred) | No sensitive data (auth tokens) exists until Phase 7; MMKV encryptionKey + expo-secure-store deferred to Phase 7 alongside the Supabase session adapter, documented not silently skipped (RESEARCH Security Domain V6) |
| T-01-09 | Information Disclosure / regulatory | Accidental diagnosis/streak field entering the local schema (GDPR Art. 9, shame-free) | mitigate | schema.denylist.test.ts structurally forbids the field-name set across all four schemas and guards future phases |
| T-01-10 | Tampering | Malformed JSON in a record key corrupting list() | mitigate | Repository read paths tolerate a missing/invalid record key (skip, do not crash); covered by CRUD tests |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx jest data --watchAll=false` passes (CRUD + isolation + settings + denylist)
- grep confirms `createMMKV` used, `new MMKV(` absent
</verification>

<success_criteria>
- All four collections persist locally in MMKV with working repository access (three CRUD repos + one settings singleton)
- Zero streak / daily-aggregate / diagnosis fields, enforced by an automated denylist guard
- MMKV encryption explicitly deferred to Phase 7 and documented (not silently skipped)
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-05-SUMMARY.md` when done.
</output>
