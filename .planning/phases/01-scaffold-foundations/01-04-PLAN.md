---
phase: 01-scaffold-foundations
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - i18n/index.ts
  - i18n/useLocale.ts
  - i18n/locales/en.json
  - i18n/locales/pl.json
  - i18n/__tests__/plurals.test.ts
  - i18n/__tests__/resolveInitialLocale.test.ts
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "i18next initializes with en + pl resources and no compatibilityJSON override, so Polish plurals resolve via CLDR (_one/_few/_many/_other) automatically (D-08)"
    - "First-launch locale resolves from the device: system language Polish -> pl, anything else -> en, with no picker screen (D-07)"
    - "The i18n layer supports runtime locale switching from day one via a useLocale() setter (D-08), even though the user-facing control ships in Phase 8"
    - "Polish plural forms render correctly for counts 0, 1, 2, 5, and 22"
  artifacts:
    - path: "i18n/index.ts"
      provides: "i18next init + device locale resolution"
      contains: "initReactI18next"
    - path: "i18n/locales/pl.json"
      provides: "Polish strings incl. _one/_few/_many plural keys"
      contains: "_few"
    - path: "i18n/locales/en.json"
      provides: "English strings"
      contains: "translation"
    - path: "i18n/useLocale.ts"
      provides: "current locale + runtime setter"
      exports: ["useLocale"]
  key_links:
    - from: "i18n/index.ts"
      to: "expo-localization getLocales()"
      via: "resolveInitialLocale()"
      pattern: "getLocales"
    - from: "i18n/useLocale.ts"
      to: "i18next changeLanguage"
      via: "runtime switch"
      pattern: "changeLanguage"
---

<objective>
Build the i18n layer (FND-05, D-07/D-08): i18next + react-i18next initialized with PL/EN resources, device-locale resolution at boot (Polish -> pl else en, no picker), CLDR-correct Polish plurals with zero compatibilityJSON override, and a runtime locale switcher ready for the Phase 8 settings control.

Purpose: Every screen must render its copy from string files with correct Polish plural forms and zero hardcoded copy. Retrofitting i18n later is painful (Pitfall 5), so it is wired from the first screen. Polish's 4 plural categories are exactly the class of rule a hand-rolled branch gets wrong (12-14 are _many despite ending 2-4) — i18next's Intl.PluralRules handles it correctly.
Output: i18n/ module (init, locales, useLocale hook) plus plural and locale-resolution tests.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-foundations/01-CONTEXT.md
@.planning/phases/01-scaffold-foundations/01-RESEARCH.md
@.planning/phases/01-scaffold-foundations/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18next init, locale resolution, locale files, and runtime switcher</name>
  <files>i18n/index.ts, i18n/useLocale.ts, i18n/locales/en.json, i18n/locales/pl.json</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (Pattern D — i18next init, resolveInitialLocale, pl.json plural block; Anti-Patterns: no compatibilityJSON; State of the Art: v4 JSON default)
    - .planning/phases/01-scaffold-foundations/01-CONTEXT.md (D-07 device-locale rule, D-08 runtime switch + CLDR)
  </read_first>
  <behavior>
    - resolveInitialLocale() returns 'pl' when getLocales()[0].languageCode === 'pl', else 'en'
    - t('sessionsRemaining', { count }) returns the correct Polish form for count in {0, 1, 2, 5, 22}
    - useLocale() exposes the current locale and a setter that calls i18next changeLanguage
  </behavior>
  <action>
Create `i18n/index.ts`: import i18n from 'i18next', initReactI18next from 'react-i18next', getLocales from 'expo-localization', and the en/pl JSON resources. Implement `resolveInitialLocale(): 'pl' | 'en'` reading `getLocales()[0]?.languageCode` and returning 'pl' only when it equals 'pl', else 'en' (D-07 — no picker). Call `i18n.use(initReactI18next).init({ resources: { en: { translation: en }, pl: { translation: pl } }, lng: resolveInitialLocale(), fallbackLng: 'en', interpolation: { escapeValue: false } })`. Do NOT set compatibilityJSON — i18next 26.x defaults to JSON v4 (CLDR plurals); setting 'v3' would break Polish pluralization (Anti-Patterns). Export the i18n instance as default and export resolveInitialLocale as a named export for testing.

Create `i18n/useLocale.ts` exporting `useLocale()` returning the current locale (from i18n.language, via react-i18next's useTranslation or a small state wrapper) and a `setLocale(next: 'pl' | 'en')` that calls `i18n.changeLanguage(next)` (D-08 runtime switch). The resolved/selected locale should also be persisted to the settings repository — since the settings store lands in Plan 05, expose a persistence hook point but do not hard-depend on Plan 05 here; Plan 06 wires persistence when it mounts providers. Document this seam in the SUMMARY.

Create `i18n/locales/en.json` and `i18n/locales/pl.json` with at least: an app-level namespace of placeholder-screen copy keys that Plan 06's shells will consume (home title + "Start a session?" offer copy, brain-dump/starter/history/settings screen titles) written per the shame-free / offer-not-instruct grammar; plus the pluralized `sessionsRemaining` key. In pl.json provide `sessionsRemaining_one`, `sessionsRemaining_few`, `sessionsRemaining_many`, and (for count 0) rely on `sessionsRemaining_many` per Polish CLDR (0 -> many) OR add `sessionsRemaining_other` as the fallback; in en.json provide `sessionsRemaining_one` and `sessionsRemaining_other`. Copy must never instruct as the mascot (offers only — "Start a session?" is an offer button, not a command).
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx tsc --noEmit 2>&1 | tail -5 && node -e "JSON.parse(require('fs').readFileSync('i18n/locales/pl.json','utf8'));JSON.parse(require('fs').readFileSync('i18n/locales/en.json','utf8'));console.log('locales valid json')"</automated>
  </verify>
  <acceptance_criteria>
    - `i18n/index.ts` calls i18n.init with resources for en and pl, lng from resolveInitialLocale(), fallbackLng 'en', and NO compatibilityJSON key
    - `resolveInitialLocale` is exported and returns 'pl' only for languageCode 'pl'
    - `i18n/locales/pl.json` contains `sessionsRemaining_one`, `sessionsRemaining_few`, and `sessionsRemaining_many`
    - `i18n/locales/en.json` and pl.json are valid JSON and share the same top-level key set (except plural-suffix variants)
    - `i18n/useLocale.ts` exports `useLocale` with a setter that calls i18n.changeLanguage
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>i18next initializes with PL/EN, resolves device locale per D-07, exposes a runtime switcher per D-08, and locale JSON files carry CLDR Polish plural keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polish plural + locale-resolution unit tests</name>
  <files>i18n/__tests__/plurals.test.ts, i18n/__tests__/resolveInitialLocale.test.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-foundations/01-RESEARCH.md (§Validation Architecture — FND-05 plural + locale-resolution rows; Pattern D plural semantics: _few for 2-4 except 12-14, _many for 0/5+/teens)
    - i18n/index.ts and i18n/locales/pl.json (from Task 1)
  </read_first>
  <behavior>
    - t('sessionsRemaining', {count:1}) -> Polish _one form
    - t('sessionsRemaining', {count:2}) -> Polish _few form
    - t('sessionsRemaining', {count:5}) -> Polish _many form
    - t('sessionsRemaining', {count:22}) -> Polish _few form (ends in 2)
    - t('sessionsRemaining', {count:0}) -> Polish _many form
    - resolveInitialLocale returns 'pl' for a mocked Polish device, 'en' otherwise
  </behavior>
  <action>
Create `i18n/__tests__/plurals.test.ts`: import the i18n instance, changeLanguage to 'pl', and assert `i18n.t('sessionsRemaining', { count: n })` returns the expected Polish string (matching the pl.json values) for n in {0, 1, 2, 5, 22}. This proves CLDR plural selection works — 22 must select _few (ends in 2, not a teen) and 5/0 must select _many. Also assert the EN forms for count 1 vs 2.

Create `i18n/__tests__/resolveInitialLocale.test.ts`: mock 'expo-localization' getLocales to return a Polish device ([{ languageCode: 'pl' }]) and assert resolveInitialLocale() === 'pl'; then mock a non-Polish device ([{ languageCode: 'de' }]) and assert === 'en'; and an empty/undefined locale list falling back to 'en'.
  </action>
  <verify>
    <automated>cd /home/user/Trinket && npx jest i18n --watchAll=false 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Plural test passes for counts 0, 1, 2, 5, 22 in Polish with the correct CLDR form each (22 -> _few, 5 -> _many, 0 -> _many)
    - Locale-resolution test passes: 'pl' device -> 'pl', 'de' device -> 'en', empty -> 'en'
    - `npx jest i18n --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Automated tests prove Polish CLDR plurals render correctly and device-locale resolution obeys D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device locale → app | Read-only device signal (languageCode) selects initial locale; no untrusted write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Hardcoded copy bypassing i18n (regulatory/shame-free risk) | mitigate | eslint i18next/no-literal-string gate (Plan 02) forces all copy through t(); locale JSON is the single copy source, reviewable for forbidden medical/shame language |
| T-01-07 | Information Disclosure | Locale signal | accept | languageCode is non-sensitive device metadata; not content, not stored off-device |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx jest i18n --watchAll=false` passes (plurals + locale resolution)
- pl.json / en.json are valid JSON with matching key sets
</verification>

<success_criteria>
- i18next renders PL/EN copy with CLDR-correct Polish plurals and no compatibilityJSON override (D-08)
- Device-locale resolution picks pl for Polish devices, en otherwise, with no picker (D-07)
- Runtime locale switching is available for the Phase 8 settings control (D-08)
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-foundations/01-04-SUMMARY.md` when done.
</output>
