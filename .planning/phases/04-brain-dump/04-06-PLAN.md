---
phase: 04-brain-dump
plan: 06
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - app.json
  - __mocks__/expo-speech-recognition.ts
  - jest.setup.ts
  - src/features/brain-dump/appendFinalSegmentToDraft.ts
  - src/features/brain-dump/__tests__/appendFinalSegmentToDraft.test.ts
  - src/features/brain-dump/useVoiceCapture.ts
  - src/features/brain-dump/__tests__/useVoiceCapture.test.tsx
  - src/app/brain-dump.tsx
  - i18n/locales/en.json
  - i18n/locales/pl.json
autonomous: true
requirements: [DUMP-02]
user_setup:
  - service: expo-speech-recognition
    why: "On-device platform STT for voice capture (DUMP-02)"
    dashboard_config:
      - task: "Run `npx expo prebuild --clean` then rebuild the dev client — first native change since Phase 2 (app.json plugin added)"
        location: "developer machine / EAS"
      - task: "D-02 device spike: verify pl-PL on-device recognition + one-utterance-per-line segmentation on real low/mid-tier Android (see 04-VALIDATION.md Manual-Only) — cannot run in this container"
        location: "physical Android device"

must_haves:
  truths:
    - "Voice is an augment over the same text field — an STT failure (unavailable, permission denied, error) never breaks capture; the text field stays fully usable (D-01, DUMP-02)"
    - "Mic permission is requested contextually on the first mic tap, never upfront (D-03)"
    - "When recognition is unavailable or the active locale isn't installed or permission is denied, the mic is hidden/disabled and a single non-alarming caption shows (DUMP-02, UI-SPEC Flag 9)"
    - "Each final utterance segment appends as a new line to the draft field, so a spoken pause ≈ a new item (D-03, subject to the D-02 device spike)"
    - "Segment-append logic is isolated in one small pure function, swappable if the device spike finds different real-hardware behavior (Pitfall 2)"
  artifacts:
    - path: "src/features/brain-dump/appendFinalSegmentToDraft.ts"
      provides: "Pure final-transcript → appended-line function"
      exports: ["appendFinalSegmentToDraft"]
    - path: "src/features/brain-dump/useVoiceCapture.ts"
      provides: "Voice-capture hook: availability probe, contextual permission, recording state, final-segment append"
      exports: ["useVoiceCapture"]
    - path: "__mocks__/expo-speech-recognition.ts"
      provides: "Jest fake of the STT module + event hook, with per-test overrides + event emitter"
    - path: "app.json"
      provides: "expo-speech-recognition config plugin (permissions + Android package visibility)"
      contains: "expo-speech-recognition"
  key_links:
    - from: "src/app/brain-dump.tsx (capture view)"
      to: "src/features/brain-dump/useVoiceCapture.ts"
      via: "hook drives mic button + recording pill; unavailable → caption + text-only"
      pattern: "useVoiceCapture"
    - from: "src/features/brain-dump/useVoiceCapture.ts"
      to: "expo-speech-recognition"
      via: "ExpoSpeechRecognitionModule + useSpeechRecognitionEvent"
      pattern: "ExpoSpeechRecognition"
---

<objective>
Ship the voice-augment slice (DUMP-02): install `expo-speech-recognition` (native, config-plugin), add its Jest mock, build an isolated final-segment appender and a `useVoiceCapture` hook that handles availability/permission/fallback, and wire a mic button + recording pill + graceful-fallback caption into the existing capture view. All fallback + permission + segment logic is unit-tested against the mock. Real on-device Polish recognition and continuous-mode segmentation are the D-02 device spike (manual, per 04-VALIDATION.md) — NOT code blockers here.

Purpose: Completes DUMP-02 while keeping D-01's text-core guarantee: the mic only ever adds a faster path, never a load-bearing dependency.
Output: STT install + config + mock, `appendFinalSegmentToDraft`, `useVoiceCapture`, capture-view wiring, voice i18n copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brain-dump/04-CONTEXT.md
@.planning/phases/04-brain-dump/04-RESEARCH.md
@.planning/phases/04-brain-dump/04-UI-SPEC.md
@.planning/phases/04-brain-dump/04-VALIDATION.md

<interfaces>
Library API (04-RESEARCH.md Pattern 4, verified against the README):
- `ExpoSpeechRecognitionModule.requestPermissionsAsync(): Promise<{ granted: boolean }>`
- `ExpoSpeechRecognitionModule.isRecognitionAvailable(): boolean`
- `ExpoSpeechRecognitionModule.supportsOnDeviceRecognition(): boolean`
- `ExpoSpeechRecognitionModule.getSupportedLocales(): { locales: string[]; installedLocales: string[] }` (NOT available Android ≤12 — probe defensively)
- `ExpoSpeechRecognitionModule.start({ lang, interimResults, continuous, requiresOnDeviceRecognition })` / `.stop()`
- `useSpeechRecognitionEvent('result', (e:{ results:{transcript:string}[]; isFinal:boolean }) => void)` and `('error', ...)`
Config plugin tuple for app.json (04-PATTERNS.md app.json section) — microphonePermission, speechRecognitionPermission, androidSpeechServicePackages:['com.google.android.googlequicksearchbox'].
Package legitimacy: `expo-speech-recognition@56.0.1` is slopcheck [OK]/Approved in 04-RESEARCH.md's Package Legitimacy Audit — no legitimacy checkpoint required; single-maintainer risk is a standing watch item, not a blocker.
Existing capture view (src/app/brain-dump.tsx, 04-03): has the multiline field + draft read/write + a reserved layout slot for the mic/recording pill. Mascot: import `Mascot` from '@/components/Mascot/Mascot'; MascotState is locked to 5 values — map `idle` before mic tap, `presence` while recording, back to `idle` on stop (UI-SPEC Flag 1); prominence hardcoded `subtle`.
Mock analogs: __mocks__/lottie-react-native.tsx + __mocks__/expo-localization.ts; registration idiom in jest.setup.ts (one `jest.mock('<pkg>')` line + comment).
Voice i18n keys to ADD (UI-SPEC §1): capture.prompt.listening/listeningSub, capture.recordingLabel, capture.micLabel.idle/active, capture.voiceUnavailable, capture.textFallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install STT, config plugin, Jest mock, voice copy</name>
  <read_first>
    - app.json (existing plugins array — bare-string + tuple forms)
    - __mocks__/lottie-react-native.tsx + __mocks__/expo-localization.ts (mock surface idioms)
    - jest.setup.ts (existing jest.mock registrations)
    - .planning/phases/04-brain-dump/04-RESEARCH.md (Code Examples → Jest mock; Config plugin)
    - i18n/locales/en.json + pl.json (brainDump.capture nesting from 04-02)
  </read_first>
  <action>Install with `npx expo install expo-speech-recognition` (resolves the SDK-56-compatible version; do not raw-`npm install`). Add the `expo-speech-recognition` config-plugin tuple to `app.json`'s `plugins` array with `microphonePermission`, `speechRecognitionPermission`, and `androidSpeechServicePackages: ["com.google.android.googlequicksearchbox"]` (exact strings per 04-PATTERNS.md). Create `__mocks__/expo-speech-recognition.ts` mirroring the lottie/localization mock pattern: export `ExpoSpeechRecognitionModule` as an object of `jest.fn()`s (`requestPermissionsAsync`→`{granted:true}`, `getPermissionsAsync`, `isRecognitionAvailable`→true, `supportsOnDeviceRecognition`→true, `getSupportedLocales`→`{locales:['en-US','pl-PL'],installedLocales:['en-US']}`, `start`, `stop`), export a `useSpeechRecognitionEvent(event, listener)` that registers listeners, and export a test-only `__emitSpeechEvent.{result,error}(...)` emitter so tests can simulate native events — each module fn individually overridable per-test via `.mockReturnValue`. Register `jest.mock('expo-speech-recognition');` in `jest.setup.ts` after the existing three native mocks (one line + explanatory comment). Add the voice i18n keys (listening prompt/sub, recordingLabel with a `{{duration}}` placeholder, micLabel.idle/active, voiceUnavailable, textFallback) to BOTH en.json and pl.json using the exact UI-SPEC EN/PL strings. Note in the SUMMARY: this is the first native change since Phase 2 — "run `npx expo prebuild --clean` after pulling."</action>
  <verify>
    <automated>node -e "const app=require('./app.json'); const has=JSON.stringify(app.expo.plugins).includes('expo-speech-recognition'); const en=require('./i18n/locales/en.json'), pl=require('./i18n/locales/pl.json'); const dig=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o); const keys=['capture.prompt.listening','capture.prompt.listeningSub','capture.recordingLabel','capture.micLabel.idle','capture.micLabel.active','capture.voiceUnavailable','capture.textFallback']; const miss=keys.filter(k=>!dig(en.brainDump,k)||!dig(pl.brainDump,k)); if(!has){console.error('plugin missing');process.exit(1)} if(miss.length){console.error('copy missing',miss);process.exit(1)} console.log('plugin + voice copy present')"</automated>
  </verify>
  <acceptance_criteria>
    - `expo-speech-recognition` appears in package.json dependencies and in app.json plugins
    - The verify script prints "plugin + voice copy present"
    - `jest.setup.ts` contains `jest.mock('expo-speech-recognition')`; `__mocks__/expo-speech-recognition.ts` exports `ExpoSpeechRecognitionModule`, `useSpeechRecognitionEvent`, and `__emitSpeechEvent`
    - `npm test` (full suite) still green with the new mock registered (no existing suite breaks)
  </acceptance_criteria>
  <done>STT installed, config plugin + mock + registration + voice copy in place; prebuild note recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: appendFinalSegmentToDraft (pure) + useVoiceCapture hook</name>
  <read_first>
    - __mocks__/expo-speech-recognition.ts (from Task 1)
    - src/features/co-pilot/useElapsedSession.ts (hook + renderHook test precedent, ref/state lessons in STATE.md)
    - .planning/phases/04-brain-dump/04-RESEARCH.md (Pattern 4; Pitfall 1; Pitfall 2)
    - data/draft.ts (write/append target)
  </read_first>
  <behavior>
    - appendFinalSegmentToDraft('a\nb', 'c') → 'a\nb\nc'; appending to '' → 'c'; trims the incoming transcript and collapses a leading space Android may insert; ignores an empty transcript (returns input unchanged)
    - useVoiceCapture reports available=false when isRecognitionAvailable() is false OR the active locale is absent from installedLocales (defensive: treat getSupportedLocales throwing as unavailable, Android ≤12)
    - First startListening() call requests permission; if `{granted:false}` the hook stays not-recording and reports unavailable/denied (single fallback signal) — text field usability is unaffected (hook exposes no blocking state)
    - On a 'result' event with isFinal, the hook appends via appendFinalSegmentToDraft and surfaces the new text to its consumer
    - An 'error' event ends recording and flips to the unavailable/fallback signal without throwing
  </behavior>
  <action>TDD. Create `src/features/brain-dump/appendFinalSegmentToDraft.ts` (pure: `(current: string, transcript: string): string`) mirroring parseDumpText's shape — isolate this per Pitfall 2 so a spike finding is a one-function fix. Write its test first (RED). Then create `src/features/brain-dump/useVoiceCapture.ts`: a hook taking the current draft text + an `onChange(text)` callback (and current locale), exposing `{ available, recording, micLabelKey, start, stop }`. `available` is computed from `isRecognitionAvailable()` + `supportsOnDeviceRecognition()`/`installedLocales` (guarded try/catch → false). `start` requests permission on first call (D-03 contextual), and on grant calls `ExpoSpeechRecognitionModule.start({ lang: locale==='pl'?'pl-PL':'en-US', interimResults:true, continuous:true, requiresOnDeviceRecognition:true })`. Register `useSpeechRecognitionEvent('result', ...)` to append final segments via `appendFinalSegmentToDraft` + `onChange`, and `('error', ...)` to stop + mark unavailable. Write `src/features/brain-dump/__tests__/useVoiceCapture.test.tsx` using `renderHook` + the mock's `__emitSpeechEvent` and per-test `.mockReturnValue` overrides to cover: available-true happy path (final segment appends a line), `isRecognitionAvailable`→false (available=false), permission denied (no recording, fallback signal), locale-not-installed (available=false), and an error event (stops, fallback) — closing Pitfall 1 entirely at the Jest layer. Commit RED test then GREEN impl.</action>
  <verify>
    <automated>npm test -- --testPathPattern="appendFinalSegmentToDraft|useVoiceCapture"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern="appendFinalSegmentToDraft|useVoiceCapture"` passes all five fallback/permission/append cases
    - `appendFinalSegmentToDraft.ts` is pure (no react/react-native/module import; grep 0 imports from those)
    - A test asserts `available===false` for both the unavailable and locale-not-installed cases, and that permission-denied yields no recording
    - RED-before-GREEN commit pair present for appendFinalSegmentToDraft
  </acceptance_criteria>
  <done>Segment-append is isolated and pure; the hook's availability/permission/error fallback is fully mock-tested (Pitfall 1 & 2 closed).</done>
</task>

<task type="auto">
  <name>Task 3: Wire mic + recording pill + fallback caption into the capture view</name>
  <read_first>
    - src/app/brain-dump.tsx (capture view + reserved mic slot from 04-03)
    - src/features/brain-dump/useVoiceCapture.ts (Task 2)
    - src/components/Mascot/Mascot.tsx + src/components/Mascot/types.ts (5-locked MascotState; subtle prominence)
    - .planning/phases/04-brain-dump/04-UI-SPEC.md (§1 Capture view; Flags 1, 3, 5, 9)
  </read_first>
  <action>In the capture view, call `useVoiceCapture(fieldText, setFieldTextAndDraft, currentLocale)`. When `available` is true: render the mic button (accent-filled pill, Unicode 🎤 glyph per Flag 3, `accessibilityLabel` from `capture.micLabel.idle|active`) that toggles `start`/`stop`; while `recording`, swap the idle prompt for `capture.prompt.listening` + `.listeningSub`, show the recording-status pill (`capture.recordingLabel` with an m:ss duration from a local formatter — reuse co-pilot's formatDuration idiom, tabular-nums) with a soft `mascotGlow` ring (Flag 5), and render `capture.textFallback` ("or type instead") as a text link that focuses the field. When `available` is false: render `capture.voiceUnavailable` in place of the mic (single shared caption for unavailable/denied/error — Flag 9) and keep the text field fully interactive. Add the `Mascot` (prominence `subtle`) mapping state `idle`→`presence`→`idle` around recording (Flag 1). Final segments already flow into the same field via the hook's onChange, so they land as new lines the user can edit before Save (D-01/D-03) — no separate transcript panel (Flag 2). All copy via `t()`, token-only styling, flatten styles. Commit as `feat(04-06): voice capture UI + graceful fallback`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=brain-dump && npm run lint:hex</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=brain-dump` green (capture + item-row + voice-hook suites)
    - Source assertions: `grep -c "useVoiceCapture" src/app/brain-dump.tsx` ≥ 1; `grep -c "voiceUnavailable\|micLabel" src/app/brain-dump.tsx` ≥ 1; no 6th MascotState string introduced (only 'idle'/'presence' used here)
    - `npm run lint:hex` passes; no `i18next/no-literal-string` violations
    - `npm run verify` full bundle green
  </acceptance_criteria>
  <done>Mic + recording pill render when STT is available; unavailable/denied/error degrade to one caption with the text field fully usable; mascot presence maps correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mic audio → on-device STT → text field | Spoken audio processed on-device; transcript enters the local draft |
| npm install → dependency tree | New native package added to the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-SC | Tampering | expo-speech-recognition install | mitigate | Package is slopcheck [OK]/Approved (04-RESEARCH.md audit), MIT, no postinstall script, peer-compatible; installed via `npx expo install` (SDK-pinned). Single-maintainer risk logged as a standing watch item, not a blocker — no legitimacy checkpoint required |
| T-04-06-NET | Information Disclosure | STT recognition path | mitigate | `requiresOnDeviceRecognition: true` on start() keeps audio/transcript on-device (no network fallback) per the on-device-preferred constraint; the D-02 device spike must confirm this is actually enforced, not just documented |
| T-04-06-PERM | Elevation of Privilege | mic permission | mitigate | Permission requested contextually on first mic tap only (D-03), never upfront; denial degrades gracefully to text-only with a non-rebuking single caption (Flag 9) |
| T-04-06-CRASH | Denial of Service | availability probe on Android ≤12 | mitigate | getSupportedLocales() is called in a try/catch that treats a throw as unavailable, so an older-Android probe failure hides the mic instead of crashing capture |
| T-04-06-INFO | Information Disclosure | transcript/item text | mitigate | Transcript flows only into the local field; no analytics/console logging of transcript or item.text (no-content-payload; analytics is Phase 8) |
</threat_model>

<verification>
- `npm run verify` full bundle green (eslint + hex + mascot-asset + jest)
- Automated STT coverage is mock-based; real pl-PL on-device recognition + one-utterance-per-line segmentation + real permission-dialog feel are the D-02 device spike (04-VALIDATION.md Manual-Only) — tracked as a separate human/device task, NOT part of this plan's automated gate
- SUMMARY must carry the `npx expo prebuild --clean` agent-rule note (first native change since Phase 2)
</verification>

<success_criteria>
- DUMP-02 code-complete: voice augments the text field; unavailable/denied/error all degrade to a fully-usable text-only capture with one non-alarming caption
- Contextual permission (D-03), on-device flag (D-01 privacy), and isolated swappable segment logic (Pitfall 2) all in place
- Device-only verification (D-02 spike) explicitly handed off, not silently skipped
</success_criteria>

<output>
Create `.planning/phases/04-brain-dump/04-06-SUMMARY.md` when done. Include the `npx expo prebuild --clean` note and flag the D-02 device spike as an open manual verification.
</output>
