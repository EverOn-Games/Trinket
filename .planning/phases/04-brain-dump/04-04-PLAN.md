---
phase: 04-brain-dump
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/app/brain-dump.tsx
  - src/app/__tests__/brainDumpItemRow.test.tsx
autonomous: true
requirements: [DUMP-03, DUMP-04]

must_haves:
  truths:
    - "Tapping an item's category chip reveals all 5 options inline; tapping one changes the category in one tap and persists it (DUMP-03, D-11)"
    - "An item can be deleted only via an explicit action with an inline confirm — never a bare swipe (D-13)"
    - "An item's text can be edited inline; the edit leaves its category unchanged (D-13)"
    - "Each item shows a 'Start a session' affordance that routes to /co-pilot with dumpItemId (DUMP-04, D-14 caller side)"
    - "Items are inert — no due dates, reminders, or badges appear on a row (DUMP-04)"
    - "A promoted item stays in the list, quietly marked, never a badge/count/completed treatment (D-15)"
  artifacts:
    - path: "src/app/brain-dump.tsx"
      provides: "DumpItemRow with category chip picker, inline edit, delete-confirm, promote button + quiet marker"
      contains: "dumpItemsRepo.update"
  key_links:
    - from: "src/app/brain-dump.tsx (DumpItemRow)"
      to: "data/repositories/dumpItems.ts"
      via: "update(id, {category}) on chip tap; update(id, {text}) on edit; remove(id) on delete"
      pattern: "dumpItemsRepo\\.(update|remove)"
    - from: "src/app/brain-dump.tsx (DumpItemRow)"
      to: "/co-pilot route"
      via: "router.push with dumpItemId param on promote"
      pattern: "dumpItemId"
---

<objective>
Turn the read-only item row from 04-03 into the full correction surface: an inline 5-option category chip picker (D-11), inline text-edit that preserves category (D-13), an explicit delete with inline confirm (D-13), and a "Start a session" promote button that navigates to `/co-pilot` with `dumpItemId` plus the quiet promoted marker (D-14 caller side / D-15). All inline, no modals, no separate screens.

Purpose: Completes DUMP-03's "changeable with one tap" and DUMP-04's inert-with-one-tap-promote on the Brain Dump side. The co-pilot side of the promote contract is built in parallel by 04-05.
Output: enhanced `src/app/brain-dump.tsx` (DumpItemRow) + a component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brain-dump/04-CONTEXT.md
@.planning/phases/04-brain-dump/04-UI-SPEC.md
@.planning/phases/04-brain-dump/04-PATTERNS.md

<interfaces>
Consumes:
- `dumpItemsRepo.update(id, partial): DumpItem`, `dumpItemsRepo.remove(id): void` (Phase 1)
- `router.push({ pathname: '/co-pilot', params: { dumpItemId: item.id } })` via `useRouter()` from expo-router
- i18n keys under `brainDump.item.*` (04-02): promote, edit, editDone, editCancel, delete, deleteConfirm.{heading,confirm,cancel}, promotedMarker; and `brainDump.category.*` for chip labels
- `DumpItem` / `DumpItemCategory` from data/types.ts; category order `['errands','work','home','people','someday']`

Row layout (UI-SPEC Screen Contracts §3):
- Line 1: category chip (tappable) + trailing `item.promotedMarker` when `item.promotedTaskId` is set
- Line 2: item text (Body, wraps up to 3 lines; dimmed via textSecondary when promoted)
- Line 3: "Edit" / "Delete" text links (Label, textSecondary, no icons) + flex spacer + "Start a session" accent pill
Chip-picker analog: co-pilot.tsx LENGTH_CHIP_VALUES.map(...) chip row — selected option accent-filled, others surfaceElevated; reuse this exact structure for the 5 category options (UI-SPEC Flag 7: collapsed chip is NOT accent-colored).
Only one of {picker-expanded, edit-mode, delete-confirm} is active per row at a time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category chip picker + inline edit + delete confirm</name>
  <read_first>
    - src/app/brain-dump.tsx (04-03 list/row baseline)
    - src/app/co-pilot.tsx lines 322-373 (LENGTH_CHIP chip-row pattern; useRef double-tap guard)
    - src/app/history.tsx lines 23-80 (in-file Row component receiving the record as a prop)
    - .planning/phases/04-brain-dump/04-UI-SPEC.md (§3 Item row, Flags 4 & 7)
  </read_first>
  <action>Extract the list's row into an in-file `DumpItemRow` component taking `item: DumpItem` plus callbacks, mirroring history.tsx's SessionRow. Add per-row local state for a mutually-exclusive `rowMode: 'idle' | 'picker' | 'edit' | 'delete'`.
  Category chip (Line 1): collapsed chip shows the current `brainDump.category.[item.category]` label on `surfaceElevated`/`textSecondary` (NOT accent — Flag 7); tapping sets `rowMode='picker'`. Picker renders all 5 category options as a compact chip row (reuse co-pilot's LENGTH_CHIP structure): the currently-selected option accent-filled, the other 4 surfaceElevated; tapping any option calls `dumpItemsRepo.update(item.id, { category })` and returns to `rowMode='idle'`.
  Inline edit: "Edit" link sets `rowMode='edit'`, swapping Line 2 for an inline `TextInput` seeded with the item text and the footer for "Done" (`item.editDone`) / "Discard changes" (`item.editCancel`). Done calls `dumpItemsRepo.update(item.id, { text })` — do NOT re-run classify (D-13: category unchanged). Discard reverts.
  Delete: "Delete" link sets `rowMode='delete'`, replacing the footer with heading `item.deleteConfirm.heading` and "Delete" (`.confirm`) / "Keep it" (`.cancel`), neutral typography (no danger color — Flag 4). Confirm calls `dumpItemsRepo.remove(item.id)` guarded by a `useRef(false)`; if it was the last item the list re-renders back to capture (D-12). Cancel returns to idle.
  Because the screen reads `dumpItemsRepo.list()` in render, force a re-render after any mutation (a `useState` bump counter or equivalent — mirror index.tsx's dismissedActiveSession re-render pattern). Token-only styling, all copy via `t()`.
  Commit as `feat(04-04): item-row category chip, edit, delete`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=brainDumpItemRow 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertions: `grep -c "dumpItemsRepo.update" src/app/brain-dump.tsx` ≥ 2 (category + text edit); `grep -c "dumpItemsRepo.remove" src/app/brain-dump.tsx` ≥ 1
    - `npm run lint:hex` passes; no `i18next/no-literal-string` violations
    - Only one row-mode active at a time (single `rowMode` state per row)
  </acceptance_criteria>
  <done>Category changes in one tap and persists; inline edit preserves category; delete requires explicit confirm.</done>
</task>

<task type="auto">
  <name>Task 2: Promote button + quiet marker + row-behavior test</name>
  <read_first>
    - src/app/brain-dump.tsx (DumpItemRow from Task 1)
    - src/app/__tests__/screens.test.tsx (renderRouter + fireEvent + contentStorage.clearAll pattern)
    - .planning/phases/04-brain-dump/04-UI-SPEC.md (§3 Promote button + Promoted quiet marker, D-15)
  </read_first>
  <action>Add the "Start a session" accent pill to DumpItemRow's Line 3 (`brainDump.item.promote`, `testID="promote-<id>"` or a stable testID), guarded by a `useRef(false)` against double-navigation, calling `router.push({ pathname: '/co-pilot', params: { dumpItemId: item.id } })`. Do NOT create a session here — that is co-pilot's job (D-14; enforced by 04-05). When `item.promotedTaskId` is set, render the trailing `brainDump.item.promotedMarker` ("· in Co-pilot") on Line 1 and dim the item text (textSecondary) — never a badge/count/completed state (D-15); the promote button stays active (re-promote allowed). Then write `src/app/__tests__/brainDumpItemRow.test.tsx` (renderRouter at `/brain-dump`, seed `dumpItemsRepo.create(...)` first, awaited fireEvent, clearAll in beforeEach): assert (a) tapping the chip then a different option updates `dumpItemsRepo.get(id).category`; (b) delete requires the confirm tap and then the item is gone from `dumpItemsRepo.list()`; (c) inline edit updates text but leaves category unchanged; (d) tapping promote navigates toward `/co-pilot` carrying `dumpItemId` (assert via router spy or resulting pathname, per STATE.md's jest.spyOn(router,'replace'/'push') lesson) and the item still appears in the list afterward (D-15). Commit as `feat(04-04): promote button, quiet marker + row tests`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=brainDumpItemRow</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=brainDumpItemRow` passes all four assertions
    - Source assertion: brain-dump.tsx contains `dumpItemId` in a `router.push` and does NOT call `sessionsRepo.create` (grep: `grep -c "sessionsRepo" src/app/brain-dump.tsx` returns 0 — no duplicated session logic, D-14)
    - Promoted item remains in `dumpItemsRepo.list()` after promote (D-15)
  </acceptance_criteria>
  <done>One-tap promote navigates with dumpItemId, no session logic duplicated, promoted item stays quietly marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Row actions → dumpItemsRepo mutations / navigation | User taps drive updates, deletes, and a cross-route navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-DELDUP | Tampering | delete / promote handlers | mitigate | `useRef(false)` guards prevent a double-delete race and double-navigation; delete requires an explicit confirm tap (no silent bare-swipe loss — D-13, "nothing lost") |
| T-04-04-DUP | Tampering | promote path | mitigate | Brain Dump never calls sessionsRepo.create (grep-asserted 0); it only pushes a router param, so session creation stays single-sourced in co-pilot.tsx (D-14) |
| T-04-04-INFO | Information Disclosure | item text in row | mitigate | Row renders text only as `<Text>`; no logging/analytics of item.text; no markup rendering (no injection surface) |
</threat_model>

<verification>
- `npm test -- --testPathPattern=brain-dump` green
- `npm run verify` passes (eslint + hex + mascot-asset + jest)
</verification>

<success_criteria>
- DUMP-03 correction (one-tap chip) and DUMP-04 inert-plus-promote (caller side) complete on the Brain Dump screen
- D-11/D-13/D-15 behaviors verified; no session logic duplicated
</success_criteria>

<output>
Create `.planning/phases/04-brain-dump/04-04-SUMMARY.md` when done.
</output>
