---
phase: 04-brain-dump
plan: 05
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/app/co-pilot.tsx
  - src/app/__tests__/screens.test.tsx
autonomous: true
requirements: [DUMP-04]

must_haves:
  truths:
    - "Navigating to /co-pilot?dumpItemId=<id> starts a session from that item via the EXISTING startFromDumpItem/beginSession — no duplicated session logic (DUMP-04, D-14)"
    - "The promoted item's promotedTaskId is set to the new session id, and the item still exists afterward (D-15)"
    - "An already-live/resumable session always wins — the dumpItemId effect does not override an in-progress session (resume-priority preserved)"
    - "Entering /co-pilot with no dumpItemId behaves exactly as before (no regression to Phase 3 flows)"
  artifacts:
    - path: "src/app/co-pilot.tsx"
      provides: "Additive dumpItemId param read + one guarded effect calling the existing startFromDumpItem"
      contains: "useLocalSearchParams"
  key_links:
    - from: "src/app/co-pilot.tsx"
      to: "startFromDumpItem (existing closure)"
      via: "useEffect gated on flowPhase==='setup' && !resumablePointer, looks up dumpItemsRepo.get(dumpItemId)"
      pattern: "startFromDumpItem"
    - from: "src/app/co-pilot.tsx"
      to: "expo-router"
      via: "useLocalSearchParams<{ dumpItemId?: string }>()"
      pattern: "useLocalSearchParams"
---

<objective>
Build the co-pilot side of the promote contract (D-14): a minimal additive edit to `src/app/co-pilot.tsx` that reads a `dumpItemId` router param and, only when the screen is genuinely in a fresh setup state, starts a session from that item by calling the ALREADY-EXISTING `startFromDumpItem` closure. No session-creation logic is duplicated or restructured. This plan is built in parallel with 04-04 against the shared contract: the router param name `dumpItemId`.

Purpose: Completes DUMP-04's one-tap promote end-to-end by reusing Phase 3's tested `beginSession` path. Verified via a route-level integration test.
Output: extended `co-pilot.tsx` + an extended `screens.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brain-dump/04-CONTEXT.md
@.planning/phases/04-brain-dump/04-RESEARCH.md
@.planning/phases/04-brain-dump/04-PATTERNS.md

<interfaces>
Existing in src/app/co-pilot.tsx (READ, do not restructure — verbatim reuse targets):
- `startFromDumpItem(item: DumpItem)` — sets isStartingSessionRef, `sessionsRepo.create({ source:'dump', taskLabel:item.text })`, `dumpItemsRepo.update(item.id, { promotedTaskId: session.id })`, `beginSession(session)`
- `beginSession(session)` — activeSessionRepo.start + setActiveSession + setFlowPhase('active')
- `resumablePointer` (useState initializer) — the "does a live session already win" gate (keep-live via reconcileActiveSession)
- `flowPhase` state ('setup' | 'active' | 'ending')
- `useRouter` already imported from 'expo-router' at line 16; `dumpItemsRepo` already imported at line 27
- `dumpItemsRepo.get(id): DumpItem | undefined` (Phase 1)

Test harness (src/app/__tests__/screens.test.tsx): `renderRouter(routeContext, { initialUrl })`; routeContext already registers `'brain-dump': BrainDumpScreen` and `'co-pilot': CoPilotScreen`; `beforeEach(() => contentStorage.clearAll())`; awaited fireEvent; router-spy assertion idiom per STATE.md Phase-3 lessons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dumpItemId param read + guarded start effect (additive)</name>
  <read_first>
    - src/app/co-pilot.tsx lines 15-136 (imports, resumablePointer, flowPhase, beginSession, startFromDumpItem)
    - .planning/phases/04-brain-dump/04-RESEARCH.md (Pattern 6 — the recommended router-param hand-off, resume-priority rule)
    - .planning/phases/04-brain-dump/04-PATTERNS.md (co-pilot.tsx EXTEND ONLY section)
  </read_first>
  <action>Add `useLocalSearchParams` to the existing `expo-router` import (alongside `useRouter`). Read `const { dumpItemId } = useLocalSearchParams<{ dumpItemId?: string }>();` near the top of `CoPilotScreen`. Add ONE new `useEffect` that fires only when `dumpItemId` is a non-empty string AND `flowPhase === 'setup'` AND `!resumablePointer` (resume-priority — an already-live session always wins, matching the existing initializer gate): inside it, `const item = dumpItemsRepo.get(dumpItemId); if (item) startFromDumpItem(item);`. Depend the effect on `dumpItemId` (and include the guards). Do NOT modify the bodies of `startFromDumpItem`/`beginSession`/`resumablePointer` — this is purely additive (new import symbol + one param read + one effect). The `isStartingSessionRef` inside startFromDumpItem already prevents a double-start if the effect and a manual tap race. Commit as `feat(04-05): co-pilot dumpItemId promote hand-off`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=screens 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertions: `grep -c "useLocalSearchParams" src/app/co-pilot.tsx` ≥ 1; the new effect references `startFromDumpItem` and is gated on `flowPhase` + `!resumablePointer` (grep shows both tokens near the effect)
    - `startFromDumpItem` and `beginSession` bodies are unchanged (no new `sessionsRepo.create` call site added — count stays as Phase 3 left it)
    - Existing screens.test.tsx still passes (no Phase-3 regression)
  </acceptance_criteria>
  <done>co-pilot reads dumpItemId and starts a session via the existing closure, respecting resume priority.</done>
</task>

<task type="auto">
  <name>Task 2: Route-level promote integration test</name>
  <read_first>
    - src/app/__tests__/screens.test.tsx (routeContext, beforeEach, existing renderRouter usage)
    - data/repositories/dumpItems.ts + data/repositories/sessions.ts (get/list/create shapes for assertions)
  </read_first>
  <action>Extend `src/app/__tests__/screens.test.tsx` with a new promote-hand-off test (do not replace the file): seed `dumpItemsRepo.create({ text:'call the bank', category:'errands' })`, capture its id, then `await renderRouter(routeContext, { initialUrl: '/co-pilot?dumpItemId=' + id })`. Assert: (a) after render, `dumpItemsRepo.get(id).promotedTaskId` is set (the existing startFromDumpItem ran); (b) a session with `source:'dump'` and `taskLabel:'call the bank'` exists via `sessionsRepo.list()`; (c) the item still exists in `dumpItemsRepo.list()` (D-15). Add a second test: with a live/keep-live activeSession pointer already seeded, entering `/co-pilot?dumpItemId=<id>` does NOT start a second session (resume wins) — assert only the pre-existing session remains. Commit as `test(04-05): promote hand-off route integration`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=screens</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=screens` passes including both new tests
    - The resume-priority test proves the dumpItemId effect does not override a live session
    - Test seeds via repos and uses `contentStorage.clearAll()` isolation
  </acceptance_criteria>
  <done>The promote path is proven end-to-end at the route level, including resume-priority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dumpItemId router param → session creation | An external route param triggers a state-changing session start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-SPOOF | Spoofing/Tampering | dumpItemId param | mitigate | The effect looks up `dumpItemsRepo.get(dumpItemId)` and only proceeds if a real local item exists; an unknown/garbage id is a silent no-op (no crash, no phantom session) |
| T-04-05-DUP | Tampering | double-start race | mitigate | `isStartingSessionRef` (existing) plus the `flowPhase==='setup' && !resumablePointer` gate prevent the effect from starting a second session or overriding a live one |
| T-04-05-REGRESS | Tampering | Phase 3 session logic | mitigate | Edit is strictly additive; startFromDumpItem/beginSession bodies untouched; existing screens.test.tsx must stay green |
</threat_model>

<verification>
- `npm test -- --testPathPattern=screens` green
- `npm run verify` passes (eslint + hex + mascot-asset + jest)
</verification>

<success_criteria>
- DUMP-04 promote is end-to-end: a dumpItemId param starts a real session via the reused Phase 3 path, item stays marked (D-15), resume priority preserved, no session logic duplicated (D-14)
</success_criteria>

<output>
Create `.planning/phases/04-brain-dump/04-05-SUMMARY.md` when done.
</output>
