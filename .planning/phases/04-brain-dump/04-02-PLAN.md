---
phase: 04-brain-dump
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/draft.ts
  - data/__tests__/draft.test.ts
  - i18n/locales/en.json
  - i18n/locales/pl.json
autonomous: true
requirements: [DUMP-01]

must_haves:
  truths:
    - "In-progress capture text round-trips through a single MMKV key and clears on demand (D-06)"
    - "A corrupted/absent draft key returns an empty string, never throws (D-06 crash-safety)"
    - "The draft key string contains none of the schema denylist stems (D-06)"
    - "Every text-capture/list/item copy key exists in both en.json and pl.json with no literal fallback needed (DUMP-01, i18n hard constraint)"
  artifacts:
    - path: "data/draft.ts"
      provides: "Thin MMKV wrapper for the single brain-dump draft key"
      exports: ["readBrainDumpDraft", "writeBrainDumpDraft", "clearBrainDumpDraft"]
    - path: "i18n/locales/en.json"
      provides: "EN copy for capture (text side), list, category, item affordances"
      contains: "brainDump.capture"
    - path: "i18n/locales/pl.json"
      provides: "PL copy, warm/plain/gender-neutral register"
      contains: "brainDump.capture"
  key_links:
    - from: "data/draft.ts"
      to: "data/mmkv.ts"
      via: "import contentStorage"
      pattern: "contentStorage"
---

<objective>
Provide the two dependency-light supports the capture screen needs: a draft-persistence wrapper over a single MMKV key (D-06 auto-restore) and all text-side i18n copy (capture idle prompt, placeholder, Save, list header, 5 category labels, item affordances). No voice copy here — that ships with the voice slice (04-06).

Purpose: Draft persistence makes DUMP-01 "nothing lost" on force-quit real; the copy keys let 04-03/04-04 render entirely through `t()` with no literal strings.
Output: `data/draft.ts` + test, extended `i18n/locales/{en,pl}.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brain-dump/04-CONTEXT.md
@.planning/phases/04-brain-dump/04-UI-SPEC.md
@.planning/phases/04-brain-dump/04-PATTERNS.md

<interfaces>
From data/mmkv.ts (already landed): `export const contentStorage = createMMKV({ id: 'trinket-content' });` with `.getString(key)`, `.set(key, value)`, `.remove(key)`.
Denylist stems that the draft key MUST avoid (data/repositories/__tests__/schema.denylist.test.ts): streak, daily, completionrate, daychain, lastactive, activedays, diagnosis, adhd.
Existing brainDump namespace (i18n/locales/en.json): only `brainDump.title` and `brainDump.description` exist today — extend this object, do not create a new top-level namespace.
CLDR Polish plural convention (if any count copy is added): `_one`/`_few`/`_many`/`_other` suffixes — none expected this phase per D-08.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draft-persistence MMKV wrapper</name>
  <read_first>
    - data/mmkv.ts (contentStorage instance)
    - data/repositories/dumpItems.ts lines 14-23 (readIndex/writeIndex try/catch idiom)
    - data/repositories/__tests__/schema.denylist.test.ts (DENYLIST_STEMS, and its beforeEach clearAll reset pattern)
    - .planning/phases/04-brain-dump/04-RESEARCH.md (Pattern 3; Pitfall 3)
  </read_first>
  <behavior>
    - writeBrainDumpDraft(x) then readBrainDumpDraft() returns x
    - readBrainDumpDraft() on an empty/absent key returns '' (never undefined, never throws)
    - clearBrainDumpDraft() then readBrainDumpDraft() returns ''
    - The key constant equals the literal string 'draft:brainDump' and passes a manual check against every DENYLIST_STEMS entry
  </behavior>
  <action>Create `data/draft.ts` exporting `readBrainDumpDraft(): string`, `writeBrainDumpDraft(text: string): void`, `clearBrainDumpDraft(): void`, wrapping `contentStorage.getString/.set/.remove` over a module-const `BRAIN_DUMP_DRAFT_KEY = 'draft:brainDump'`. `readBrainDumpDraft` returns `contentStorage.getString(KEY) ?? ''`. This is a plain string value — NOT a repo record, NOT JSON-indexed (D-06). Add a header comment stating the key is verified clean against the denylist stems. Then write `data/__tests__/draft.test.ts` covering the four behaviors, using `contentStorage.clearAll()` in `beforeEach` for isolation (mirror schema.denylist.test.ts's reset). Add one assertion that lowercasing the key contains none of the 8 denylist stems.</action>
  <verify>
    <automated>npm test -- --testPathPattern=draft</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=draft` passes all four behaviors
    - Source assertion: `grep -c "draft:brainDump" data/draft.ts` returns at least 1; the test's denylist-stem assertion is green
    - `data/draft.ts` imports `contentStorage` from `./mmkv` and nothing from react/react-native
  </acceptance_criteria>
  <done>Draft wrapper round-trips, is crash-safe, and its key is provably denylist-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Text-side, list, category, and item i18n copy</name>
  <read_first>
    - i18n/locales/en.json (existing brainDump + coPilot nesting depth)
    - i18n/locales/pl.json (existing 4-form plural precedent, PL register)
    - .planning/phases/04-brain-dump/04-UI-SPEC.md (Copywriting Contract + Screen Contracts tables — use the exact EN/PL strings)
  </read_first>
  <action>Extend the `brainDump` object in BOTH `i18n/locales/en.json` and `i18n/locales/pl.json` with the exact copy from the UI-SPEC (do not invent new copy):
  Capture (text side only — voice keys are 04-06's): `capture.prompt.idle` ("What's on your mind?" / "Co masz w głowie?"), `capture.prompt.idleSub` ("Tap the mic, or start typing." / "Stuknij mikrofon albo zacznij pisać."), `capture.placeholder` (the "Errands, calls, that thing about deposits…" string / its PL from the spec), `capture.save` ("Save these" / "Zapisz wpisy").
  List: `list.captureAffordance` ("Add to your brain dump" / "Dodaj do zrzutu myśli"), `list.captureAffordanceSub` ("Speak or type." / "Powiedz albo napisz.").
  Category (5 keys): `category.errands`/`work`/`home`/`people`/`someday` (EN: Errands/Work/Home/People/Someday; PL: Do załatwienia/Praca/Dom/Ludzie/Kiedyś).
  Item: `item.promote` ("Start a session" / "Zacznij sesję"), `item.edit` ("Edit"/"Edytuj"), `item.editDone` ("Done"/"Gotowe"), `item.editCancel` ("Discard changes"/"Odrzuć zmiany"), `item.delete` ("Delete"/"Usuń"), `item.deleteConfirm.heading` ("Delete this?"/"Usunąć to?"), `item.deleteConfirm.confirm` ("Delete"/"Usuń"), `item.deleteConfirm.cancel` ("Keep it"/"Zostaw"), `item.promotedMarker` ("· in Co-pilot" / "· w Co-pilot").
  Keep register warm/plain/gender-neutral; offers-never-instructs. Mark the PL category label "Do załatwienia" with a code comment or SUMMARY note as draft-pending-native-review (UI-SPEC Flag 6).</action>
  <verify>
    <automated>node -e "const en=require('./i18n/locales/en.json'), pl=require('./i18n/locales/pl.json'); const keys=['capture.prompt.idle','capture.prompt.idleSub','capture.placeholder','capture.save','list.captureAffordance','list.captureAffordanceSub','category.errands','category.work','category.home','category.people','category.someday','item.promote','item.edit','item.editDone','item.editCancel','item.delete','item.deleteConfirm.heading','item.deleteConfirm.confirm','item.deleteConfirm.cancel','item.promotedMarker']; const dig=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o); const miss=keys.filter(k=>!dig(en.brainDump,k)||!dig(pl.brainDump,k)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('all',keys.length,'keys present in en+pl')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify node script prints "all 20 keys present in en+pl" and exits 0
    - Both JSON files remain valid (the require() above succeeds)
    - No literal category/affordance strings will be needed in JSX (keys cover every UI-SPEC text-side element)
  </acceptance_criteria>
  <done>All text-capture/list/category/item copy exists in both locales, sourced verbatim from the UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User draft text → MMKV contentStorage | Arbitrary in-progress text persisted locally on device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-CRASH | Denial of Service | readBrainDumpDraft on corrupt/absent key | mitigate | Returns `?? ''`; no JSON.parse on the plain string value, so a partial/garbled value cannot throw on restore |
| T-04-02-SCHEMA | Tampering | draft MMKV key name | mitigate | Key literal 'draft:brainDump' manually + test-asserted clean against all 8 denylist stems; the denylist test cannot see raw MMKV keys, so this is enforced by the plan's explicit assertion |
| T-04-02-INFO | Information Disclosure | draft text at rest | accept | Local-first per constraint; MMKV encryption is deferred to Phase 7 (documented in data/mmkv.ts); no analytics/logging touches this key |
</threat_model>

<verification>
- `npm test -- --testPathPattern=draft` green
- `npm run verify` passes (eslint, hex gate, jest) — i18next/no-literal-string unaffected since no JSX changed here
</verification>

<success_criteria>
- Draft wrapper is round-tripping, crash-safe, denylist-clean
- All text-side/list/category/item copy present in en+pl, verbatim from UI-SPEC
- 04-03 can build the capture + list screen entirely against `t()` keys and the draft API
</success_criteria>

<output>
Create `.planning/phases/04-brain-dump/04-02-SUMMARY.md` when done.
</output>
