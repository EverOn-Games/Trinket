---
phase: 04-brain-dump
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/app/brain-dump.tsx
  - src/app/__tests__/brainDumpCapture.test.tsx
autonomous: true
requirements: [DUMP-01, DUMP-05]

must_haves:
  truths:
    - "A user can type a multiline stream, tap Save, and see each non-blank line saved as a separate DumpItem (DUMP-01, D-04/D-05/D-07)"
    - "Each saved item lands in its classified category section; the list is grouped by the 5 categories in fixed order with empty sections omitted (D-12)"
    - "Nothing persists until Save; an empty/whitespace Save is a silent no-op (D-05/D-07)"
    - "In-progress text auto-restores from the draft key on mount and clears on Save (D-06)"
    - "Zero items routes straight to the capture view; a persistent capture affordance is always available on the list (D-12)"
    - "Brain dump remains reachable from Home in ≤2 taps — the existing Link is not regressed (DUMP-05)"
  artifacts:
    - path: "src/app/brain-dump.tsx"
      provides: "Capture view + grouped SectionList list with a viewPhase state machine"
      min_lines: 120
    - path: "src/app/__tests__/brainDumpCapture.test.tsx"
      provides: "Happy-path capture→save→grouped-list test"
  key_links:
    - from: "src/app/brain-dump.tsx"
      to: "src/features/brain-dump/parseDumpText.ts + classify.ts"
      via: "Save handler calls parseDumpText then classify per line"
      pattern: "parseDumpText|classify"
    - from: "src/app/brain-dump.tsx"
      to: "data/repositories/dumpItems.ts"
      via: "dumpItemsRepo.create per parsed line, dumpItemsRepo.list() in render"
      pattern: "dumpItemsRepo\\.(create|list)"
    - from: "src/app/brain-dump.tsx"
      to: "data/draft.ts"
      via: "read on mount, write on change, clear on Save"
      pattern: "BrainDumpDraft"
---

<objective>
Rewrite the Brain Dump stub into the load-bearing text-core screen: a `viewPhase: 'capture' | 'list'` state machine. The capture view is a single big multiline field with draft auto-restore and a Save that parses → classifies → persists each line. The list view is a `SectionList` grouped by the 5 categories (empty-suppressed) with a persistent capture affordance and empty-state routing straight into capture. No voice yet (text-core, D-01) — the mic arrives in 04-06.

Purpose: This is the first end-to-end user-visible capability of the phase — a user clears their head as text and sees it organized. It delivers DUMP-01 and preserves DUMP-05.
Output: rewritten `src/app/brain-dump.tsx` + a route-level happy-path test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brain-dump/04-CONTEXT.md
@.planning/phases/04-brain-dump/04-UI-SPEC.md
@.planning/phases/04-brain-dump/04-PATTERNS.md

<interfaces>
Consumes (all landed by prior waves / Phase 1):
- `parseDumpText(raw: string): string[]` from `@/features/brain-dump/parseDumpText` (04-01)
- `classify(text: string, locale: Locale): DumpItemCategory` from `@/features/brain-dump/classify` (04-01)
- `readBrainDumpDraft()/writeBrainDumpDraft(t)/clearBrainDumpDraft()` from `data/draft` (04-02)
- `dumpItemsRepo.create({ text, category })`, `dumpItemsRepo.list(): DumpItem[]` (Phase 1) — create spreads `...input` first, sets fresh id/createdAt
- i18n keys under `brainDump.capture.*`, `brainDump.list.*`, `brainDump.category.*` (04-02)
- `DumpItem`, `DumpItemCategory`, `Locale` from data/types.ts
- Current locale via `useTranslation().i18n.language` (as elsewhere in the app)

Screen conventions (mirror existing files):
- `src/app/history.tsx` — empty-state-vs-list branch, in-file Row component, `useState(() => Date.now())` lazy-init idiom
- `src/app/co-pilot.tsx` — `useState` viewPhase state machine under one `<Screen>`; repo read directly in render body; `useRef(false)` double-tap guard; `StyleSheet.flatten([...])` never bare arrays; token-only `useTheme()` styling
- Fixed category order: `['errands','work','home','people','someday']`; group into `{ title, data }[]` and filter out empty sections (D-12), mirroring co-pilot SetupPhase's `dumpItems.length > 0 &&` empty-suppression
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing happy-path capture→save→grouped-list test (RED)</name>
  <read_first>
    - src/app/__tests__/screens.test.tsx (renderRouter routeContext, beforeEach contentStorage.clearAll, awaited fireEvent pattern)
    - data/repositories/dumpItems.ts (list() shape for assertions)
    - .planning/phases/04-brain-dump/04-RESEARCH.md (Validation Architecture → Test Map, DUMP-01 row)
  </read_first>
  <action>Create `src/app/__tests__/brainDumpCapture.test.tsx` using `renderRouter` with `initialUrl: '/brain-dump'` (mirror screens.test.tsx's routeContext + `beforeEach(() => contentStorage.clearAll())`). Write a happy-path test: render, type a 3-line stream (including one blank line to prove drop) into the capture multiline field (found by its placeholder or a `testID="brain-dump-input"`), press the Save button (by `brainDump.capture.save` text / a `testID="brain-dump-save"`), then assert `dumpItemsRepo.list()` contains exactly the non-blank lines as separate items each with a valid category, and that the rendered list shows the item texts under category section headers. AWAIT every `fireEvent` (v14 async — per STATE.md Phase-3 lesson). Also add a no-op test: Save with a whitespace-only field creates zero items. This test fails first (screen not yet built). Commit as `test(04-03): brain-dump capture happy path`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=brainDumpCapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and FAILS on first run for the right reason (missing capture UI / no items created), not a syntax/import error
    - Test uses awaited `fireEvent` calls and `contentStorage.clearAll()` isolation
  </acceptance_criteria>
  <done>A red happy-path test pins DUMP-01's capture→save→grouped-list behavior before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Capture view + grouped SectionList (GREEN)</name>
  <read_first>
    - src/app/brain-dump.tsx (current stub to replace)
    - src/app/history.tsx (empty-state branch, in-file Row, lazy Date.now())
    - src/app/co-pilot.tsx (viewPhase state machine, repo-read-in-render, flatten styles, double-tap ref)
    - .planning/phases/04-brain-dump/04-UI-SPEC.md (Screen Contracts §1 Capture, §2 Grouped list)
  </read_first>
  <action>Rewrite `src/app/brain-dump.tsx` as a `viewPhase: 'capture' | 'list'` machine under one `<Screen>`, header docblock citing D-04/D-06/D-07/D-12/DUMP-01/DUMP-05.
  Capture view: kicker (reuse `brainDump.title` uppercased via style), idle prompt heading `brainDump.capture.prompt.idle` + subcopy `.idleSub`, a big multiline `TextInput` (`multiline`, no char limit, `testID="brain-dump-input"`) whose value is component state seeded on mount from `readBrainDumpDraft()`; on every change call `writeBrainDumpDraft(next)`; placeholder `brainDump.capture.placeholder`; a Save button (`testID="brain-dump-save"`, accent-filled pill, label `brainDump.capture.save`) guarded by a `useRef(false)` to prevent double-submit. On Save: run `parseDumpText(text)`; if empty → no-op (return, no items, no error); else for each line `dumpItemsRepo.create({ text: line, category: classify(line, currentLocale) })`, then `clearBrainDumpDraft()`, reset the field, and set `viewPhase='list'`. Reserve vertical space per the UI-SPEC spacing (mic/recording pill are NOT added this plan — leave that layout slot for 04-06).
  List view: read `dumpItemsRepo.list()` directly in render; if zero items, render the capture view instead (D-12 empty routing); else a `SectionList` whose `sections` come from a `groupByCategory(items)` helper (fixed order `errands,work,home,people,someday`, filter out empty sections), `renderSectionHeader` using `t('brainDump.category.' + section.title)` (Label style, uppercase), `renderItem` a simple in-file row showing `item.text` (Body). Add a `ListHeaderComponent` persistent capture affordance card (`brainDump.list.captureAffordance` + `.captureAffordanceSub`) that sets `viewPhase='capture'` on press. Token-only styling via `useTheme()`, all copy via `t()`, `StyleSheet.flatten([...])` for combined styles. Keep the item row minimal here — category chips / delete / edit / promote are 04-04's job.
  Commit as `feat(04-03): brain-dump text capture + grouped list`.</action>
  <verify>
    <automated>npm test -- --testPathPattern=brainDumpCapture</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=brainDumpCapture` passes (both happy path and whitespace no-op)
    - Source assertions: `grep -c "parseDumpText\|classify\|dumpItemsRepo.create\|clearBrainDumpDraft" src/app/brain-dump.tsx` ≥ 4; `grep -c "SectionList" src/app/brain-dump.tsx` ≥ 1
    - `npm run lint:hex` passes (no hex literals — token-only)
    - No `i18next/no-literal-string` violations (all copy via `t()`)
    - `src/app/index.tsx` still contains `<Link href="/brain-dump"` (DUMP-05 non-regression — grep unchanged)
  </acceptance_criteria>
  <done>Typing a stream and Saving creates categorized items and lands on the grouped list; empty state routes to capture; DUMP-05 link intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture TextInput → dumpItemsRepo / draft key | User free-text crosses into local persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-DUP | Tampering | Save handler | mitigate | `useRef(false)` double-tap guard prevents a rapid double-Save creating duplicate items (mirrors co-pilot's isStartingSessionRef) |
| T-04-03-INFO | Information Disclosure | item/draft text | mitigate | No analytics/console logging of `item.text` or field content anywhere in the screen (no-content-payload; analytics is Phase 8) |
| T-04-03-CRASH | Denial of Service | draft restore on mount | mitigate | Restore reads via readBrainDumpDraft() which returns '' on corrupt/absent state — a partial draft cannot crash capture on reopen |
| T-04-03-VALID | Input Validation | parseDumpText/classify on adversarial lines | accept | Covered by 04-01's defensive tests; screen passes raw lines straight through, no unsafe rendering (text rendered as Text, never markup) |
</threat_model>

<verification>
- `npm test -- --testPathPattern=brain-dump` green (capture suite + prior pure-fn suites)
- `npm run verify` passes end-to-end (eslint + hex + mascot-asset + jest)
</verification>

<success_criteria>
- DUMP-01 end-to-end: type stream → Save → separate categorized items visible, grouped, empty-suppressed
- D-05/D-06/D-07/D-12 behaviors hold (Save-only persistence, draft restore/clear, blank-drop, empty→capture routing)
- DUMP-05 verified non-regressed
</success_criteria>

<output>
Create `.planning/phases/04-brain-dump/04-03-SUMMARY.md` when done.
</output>
